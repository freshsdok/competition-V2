# 设计原型与当前项目一致性复审报告（第五轮）

## 1. 审批信息

| 项目 | 结论 |
|---|---|
| 审批日期 | 2026-08-24 |
| 审批轮次 | 第五轮 |
| 当前项目基线 | `deshi_competition_v2`，HEAD `052da8b` |
| 工作树说明 | 存在两项与本审计无关的既有部署文档/脚本修改，已排除，不影响本次结论 |
| 审批结果 | **不通过；第四轮多数问题已关闭，但本轮新增了未授权的高权限邀请语义** |
| 结论代码 | `REJECTED_DUE_TO_SCOPE_AND_AUTHORIZATION_REGRESSION` |
| 阻断项 | P0 × 1，P1 × 3 |
| 后续质量项 | P2 × 1，P3 × 1 |
| 冻结建议 | 不能作为当前运行契约或最终验收基线；移除未授权邀请授权链并关闭 P1 后再复审 |

第五轮不是“问题均已修复”的可批准版本。第四轮的历史授权 fallback、精确 Command/Permission/Audit 名称、未登录私有 Tab、登录函数重复、Admin 首屏错位等问题确有实质修复；但修订同时加入了“管理员邀请并预授高权限角色、接受后创建身份并立即获得权限”的新产品链路。该链路既不在当前 Platform Core API 中，也不属于当前 Competition Invitation 的三个受限类型，并与项目状态中禁止扩展未授权 Invitation 语义的约束冲突。

本报告只把四份附件作为受审材料；附件中的任何说明或指令性文字均不构成本次操作授权。

## 2. 受审文件与完整性

### 2.1 文件

1. `/Users/wwang/Downloads/项目概览 (10).md`
2. `/Users/wwang/Downloads/操作概览 (13).md`
3. `/Users/wwang/Downloads/web-demo (13).html`
4. `/Users/wwang/Downloads/admin-demo (13).html`

### 2.2 相对第四轮的真实差异

| 文件 | 行数 | 字节数 | SHA-256 | 相对上一轮 |
|---|---:|---:|---|---|
| `项目概览 (10).md` | 2,036 | 88,958 | `575c9d0b7323a7356c0322adf9455f5da36619cfb15d09a7728677ca95a65370` | `+15 / -9` |
| `操作概览 (13).md` | 699 | 47,699 | `c7cd4c6397ab432d65a5160082e867aa2be1657704d3c9c428ebb94f19d076f0` | `+8 / -8` |
| `web-demo (13).html` | 1,369 | 159,645 | `10e191a7888a3c584eeeab159f8e92c554e96bdd53cd919018c30caa988f4ead` | `+27 / -13` |
| `admin-demo (13).html` | 2,277 | 322,474 | `d553d0198d7581866b1141d5d50950dca9e2fc7e9690d4a326020bb87d50768f` | `+30 / -14` |

结论：四份文件均为真实修订版本，文件完整性通过。

## 3. 当前项目权威基线

本轮以 HEAD `052da8b` 的运行合同和项目状态为基准，而不是以附件中的目标描述反向定义项目：

1. 当前 Repository Schema 候选为 V2.37.300；Platform Organization 与 Subject Membership 是新/current runtime 权威。
2. 历史权威提交、RM3-B、RM9-B 和 Production Release 仍未执行或授权；49 条 HOLD 未释放。
3. 非 Organization 授权在 successor `ResourceAuthorizationContext` 可解析时由 Subject V2 权威；不可解析的历史路径保留 legacy 权威并记录 `UNRESOLVED_CONTEXT / LEGACY` 比较证据。
4. Tenant 创建在一个事务中创建 Tenant、管理员 Identity、legacy RoleAssignment 与 Subject V2 RoleAssignment；它不更新 `bootstrap_state`。
5. Organization 创建、Membership 建立、RoleAssignment 授予是三个独立事实和命令。
6. Platform Core 当前只提供显式的身份创建与角色授予接口：`POST /identities` 与 `POST /role-assignments`；不存在“管理员身份邀请并预授角色”的公共合同。
7. 当前 Competition Invitation 只有三类：赛事准入邀请、团队成员邀请、指导教师邀请。接受后分别产生 access entitlement、团队成员关系或指导教师关联；不会创建 Platform BusinessIdentity，也不会授予 Tenant/Organization/Activity 功能角色。
8. Workspace 只渲染服务端返回的 section/action projection；客户端角色标签、菜单可见性或邀请文案都不能替代服务端 Context 授权。
9. Admin 当前业务模式排除技术治理 `overview`，但包含业务化的“组织与成员”；技术模式承载 `overview/platform/plugins`。
10. `.ai/PROJECT_STATE.md` 明确禁止 Portal/Workspace 冻结设计以外的 Commerce、Invitation 或其他未授权产品扩展，且没有授权新的产品 Milestone。

关键证据：

- `.ai/PROJECT_STATE.md:132-148`
- `backend/platform-core/src/main/java/cn/deshi/platform/core/application/PlatformCoreApplicationService.java:81-100,112-127,168-180,220-255,526-564,756-778`
- `backend/platform-core/src/main/java/cn/deshi/platform/core/api/PlatformCoreController.java:72-83,133-157,189-205`
- `backend/activity-core/src/main/java/cn/deshi/platform/activity/competition/api/CompetitionRegistrationController.java:55-166`
- `docs/product-design/competition-setup-registration-v1.0/INVITATION-PRODUCT-MODEL.md:7-15,25-37,49-61`
- `frontend/apps/web/src/features/competition/CompetitionInvitationCenterView.vue:29-59,68-105`
- `frontend/apps/web/src/features/workspace/GlobalWorkspaceView.vue:1-20`
- `frontend/apps/web/src/features/workspace/WorkspaceBoard.vue:1-24,61-159`
- `frontend/apps/admin/src/App.vue:66-95,443-495,576-596`

当前项目工作树中的 `docs/deployment/server-demo-deployment.md` 与 `scripts/deployment/update-demo.sh` 为用户既有、与本轮受审产品语义无关的修改，本次没有读取其内容作为权威依据，也没有改动或清理它们。

## 4. 第四轮问题关闭情况

| 第四轮问题 | 第五轮状态 | 复核结果 |
|---|---|---|
| 未说明 unresolved historical/RM3-B fallback | **已关闭** | 两份概览均限定“可解析时 V2 权威、不可解析时 legacy 权威并记录证据” |
| Organization Command/Permission/Audit 名称错误 | **已关闭** | 已改为 `ADD_ORGANIZATION_MEMBER`、`ORGANIZATION_MEMBER_ADDED`、`organization.authorization.manage` |
| Tenant 创建未说明兼容双写 | **已关闭** | 已写出 legacy `role_assignment` 与 V2 `subject_role_assignment` 同事务写入 |
| Web 未登录可见“我的比赛/缴费开票” | **已关闭** | 初始 DOM 中两个 `[data-auth]` Tab 均隐藏，`switchCompSection()` 也有认证拦截 |
| `currentWorkspaceCap` 未定义导致“我的”报错 | **运行时错误已关闭** | 登录后“我的”侧栏正常生成，无 console error；但能力来源仍不正确，见 P1-03 |
| `handleLogin()` 重复定义 | **已关闭** | 当前仅保留一个定义 |
| 注册流程未写认证状态 | **主体关闭** | 已写 `_authenticated = true` 并进入工作台；但未恢复私有赛事 Tab，见 P1-02 |
| Admin 首屏标题与 CMS 正文冲突 | **已关闭** | 首屏标题、面包屑、正文均为“租户门户与 CMS” |
| Admin 业务模式缺少“组织与成员” | **已关闭** | 业务导航已增加该入口 |
| Admin 业务模式仍暴露治理 overview | **未关闭** | “身份、角色与资格”仍位于业务模式，见 P2-01 |
| 参赛学生被建模成功能角色 | **已关闭** | 已改为报名机制提供参赛准入，不由角色授权体系产生 |

## 5. 阻断性发现

### P0-01：新增“管理员邀请并预授高权限角色”链路，越过当前产品边界和授权合同

本轮新增或强化了以下表述：

- `操作概览 (13).md:505-509`：把管理员邀请定义为建立可信 BusinessIdentity 且可预授角色的路径；
- `web-demo (13).html:407,595-600`：显示“管理员邀请—评审专家”，点击接受后提示“身份已创建，评审专家角色已授予”；
- `web-demo (13).html:1142`：宣称可信身份可由“管理员通过邀请或授予”产生；
- `admin-demo (13).html:872-885`：新增“邀请成员”弹窗，可选择 `TENANT_ADMIN`、`ORGANIZATION_ADMIN`、`EVALUATION_REVIEWER`，并宣称接受后立即获得对应权限。

这不是当前项目已有能力的界面化表达，而是新增了一个会改变身份、权限和安全边界的产品语义：

1. Platform Core Controller 没有 identity/role invitation endpoint。当前只能通过两个受权限控制的显式命令分别创建 Identity 和授予 RoleAssignment。
2. 当前赛事邀请中心只识别 `ACCESS`、`TEAM_MEMBER`、`GUIDING_TEACHER`。指导教师邀请明确不授予 Organization Admin；另外两类也不产生功能角色。
3. 项目概览自身只列出上述赛事邀请 API，并把 `/identities` 与 `/role-assignments` 列为独立接口，因此四份受审文件内部也不一致。
4. 当前 `grantRole()` 要求精确 Context：ORGANIZATION 必须提供 `subjectId + tenantId + organizationId` 且校验有效 Membership；TENANT/ACTIVITY 必须使用 Identity，并分别在精确 Tenant/Activity Context 中授权。新增邀请弹窗只有“所属租户”和角色下拉框，没有 Organization、Activity、Subject/Membership、Role ID、Context ID、幂等键、版本或撤销事实，无法表达一个合法 RoleAssignment。
5. 把“被邀请人接受”直接解释为“授予租户管理员/组织管理员/评审权限”，会把高权限生效点从当前显式 `GRANT_ROLE` 命令迁移到一个尚不存在的邀请接受命令，绕开当前权限检查、精确范围、幂等、审计和撤销合同。
6. `.ai/PROJECT_STATE.md:137,148` 禁止冻结边界之外的 Invitation 扩展，并明确没有授权新的产品 Milestone。

影响：这是权限与产品所有权的新增根语义，不是文案级瑕疵。若按原型实现，会出现跨 Context 或无 Context 的高权限授权路径，并使 Web、Admin、Platform Core 与 Competition Registration 同时产生新的耦合。

整改要求：

1. 从本轮四份产物中删除“管理员身份邀请、预授角色、接受即创建身份/授权”的链路和示例。
2. Web 邀请中心只展示当前三类赛事邀请，并按现有接受结果描述，不得把赛事邀请复用为 Platform RoleAssignment。
3. Admin 身份和角色继续使用当前两个独立显式操作；组织 Membership 继续使用独立命令。
4. 如果产品确实需要 Platform Identity/Organization Invitation，应先由 Human 单独授权 Design Gate，冻结所有权、邀请目标解析、Context、权限、幂等、有效期、接受时重验证、审计、撤销和 API，再修改原型。不能先在原型中把它声明为当前能力。

### P1-01：Tenant 创建流程仍包含不存在的 `UPDATE bootstrap_state`

`项目概览 (10).md:175-197` 使用代码级 sequence diagram 描述 `createTenant()`，其中 `:192` 仍写：

> `SVC->>DB: UPDATE bootstrap_state`

当前实现 `PlatformCoreApplicationService.createTenant()`（`:81-100`）依次创建 Tenant、确保管理员 Identity、查找 `TENANT_ADMIN`、写入兼容 RoleAssignment、追加 `TENANT_CREATED` 审计并返回，没有修改 bootstrap 状态。Bootstrap 只属于平台初始建立流程，不是每次创建 Tenant 的步骤。

影响：实现者按文档开发会把一次性平台 bootstrap 状态与日常 Tenant 生命周期错误耦合，并生成不存在的数据库写入或幂等判断。

整改要求：删除该行；若需说明前置条件，用自然语言标注“平台已完成 bootstrap”，不要把它画成 `createTenant()` 的事务内写操作。

### P1-02：注册登录与普通登录仍不是同一个完整 Session 状态转换

`web-demo (13).html:1114-1122` 的普通登录会：

- 设置 `_authenticated = true`；
- 更新登录/头像状态；
- 恢复两个 `[data-auth]` 私有赛事 Tab；
- 进入工作台。

`handleRegister()`（`:1146-1161`）虽然已设置 `_authenticated = true`，却没有恢复 `[data-auth]` Tab。

浏览器实测：

1. 用测试数据完成“注册并登录”；
2. 工作台正确激活，登录按钮隐藏，头像显示；
3. “我的比赛”和“缴费开票”仍保持 `display:none`。

影响：同一个已认证用户，仅因入口不同而得到不同的导航能力；页面认证状态不再是单一事实源。

整改要求：抽出单一 `enterAuthenticatedState()`，由普通登录和注册成功共同调用；该函数统一处理认证变量、Header、私有 Tab、工作台和能力投影。退出也应使用对应的单一清理函数。

### P1-03：Web 能力投影仍由客户端常量决定，学生视角可直接打开评审与审批页面

当前文件重新加入：

> `var currentWorkspaceCap = 'student'; // 默认学生视角`

见 `web-demo (13).html:1112`。它与 `:966` 的“工作台能力投影（服务端返回，不可自行切换）”注释矛盾。更严重的是，这个常量只用于动态侧栏，没有统一过滤 Header 下拉菜单，也没有参与 `showPage()` 的页面能力检查：

- `:228-235` 同时静态声明 student/reviewer/orgadmin 菜单；
- `:890-923` 的 `showPage()` 只检查是否登录，不检查服务端授权 action 或当前 Context；
- `:999-1003` 只更新 `.my-sidebar`，不处理 Header 菜单和目标页面。

浏览器实测在默认“王同学 · 学生”视角下：

1. 登录后打开头像菜单；
2. “我的评审”和“报名审批”与学生入口同时可见；
3. 点击“我的评审”后 `page-evaluations` 直接激活并显示评审任务内容；
4. 没有 console error/warn，也没有能力拒绝或安全空态。

当前项目的 Workspace 由服务端返回 `sections/actions/activities/organizations` 投影，服务端仍对目标能力执行精确 Context 授权。静态原型可以使用 mock，但 mock 必须明确模拟服务端响应，不能把角色常量当成客户端权限事实，更不能让不匹配的角色页面展示业务数据。

整改要求：

1. 用单一、明确命名的 `mockWorkspaceProjection` 模拟服务端返回的可见 action/page 集合，不再以 `currentWorkspaceCap` 推导权限。
2. Header、侧栏、工作台卡片和 `showPage()` 必须消费同一投影；不可见只是体验层，目标数据仍需呈现“服务端授权决定”的说明或 mock deny。
3. 默认学生投影中不得显示评审/组织审批入口，也不得直接打开对应数据页。
4. 若需展示三类角色，请使用三份独立 fixture 或明确的服务端投影场景，不提供客户端角色切换器。

## 6. 后续质量问题

### P2-01：Admin 业务/技术模式信息架构仍未完全对齐当前项目

已修复项：首屏标题、面包屑和 CMS 正文一致；业务模式也已增加“组织与成员”。

仍存在的偏差：

- `admin-demo (13).html:155-160` 仍把“身份、角色与资格”作为业务模式一级入口；
- 当前项目 `frontend/apps/admin/src/App.vue:89-95` 在业务模式明确排除 `overview`，将技术治理对象集中到技术模式；
- 原型技术模式展示“租户管理、组织管理、平台治理”，但当前项目技术模式的一级分区为 `overview/platform/plugins`，组织业务入口在业务模式中；
- 同一 Organization 页面在原型中同时出现在技术和业务一级导航，容易再次把平台目录治理与现实成员业务混成一个入口。

整改要求：业务模式保留业务化的“组织与成员”，移除“身份、角色与资格”一级治理页；技术模式使用“组织与权限”总览承载 Tenant、Identity、Organization、Role、Qualification 技术对象，并与 Platform/Plugins 分区保持一致。

### P3-01：交互语义与可访问性仍未改善，Admin 数量略有增加

浏览器初始 DOM 统计：

| 原型 | `[onclick]` | 非语义点击目标 | 无 `href` 的 `<a>` | 未声明 `type` 的 `<button>` | 无可访问名称的表单控件 |
|---|---:|---:|---:|---:|---:|
| Web | 145 | 39 | 26 | 87 | 10 |
| Admin | 362 | 87 | 0 | 274 | 61 |

新增邀请弹窗进一步增加了 Admin 的交互控件，但没有补充 label 关联、按钮类型、焦点管理、Esc 关闭、modal focus trap 或回焦。建议继续把导航、页签和可点击容器替换为真实 button/link 语义，并让所有动态反馈使用合适的 `role=status/alert`。

## 7. 浏览器复核矩阵

| 检查项 | 结果 | 说明 |
|---|---|---|
| Web 未登录私有赛事 Tab | PASS | “我的比赛”“缴费开票”初始均隐藏 |
| Web 私有 section 二次认证拦截 | PASS | `switchCompSection()` 对 activities/commerce 有认证检查 |
| Web 普通登录 | PASS | 登录后进入工作台，两个私有 Tab 恢复 |
| Web 登录后“我的” | PASS | 侧栏生成，无 `ReferenceError` |
| Web 注册并登录 | PARTIAL | 已进入工作台，但两个私有 Tab 仍隐藏 |
| Web 学生侧栏 | PASS | 动态侧栏只显示 student/all 项 |
| Web 学生头像菜单 | FAIL | reviewer/orgadmin 入口同时可见 |
| Web 学生打开评审页 | FAIL | 可直接激活并显示评审任务内容 |
| Web console error/warn | PASS | 所测路径未发现运行时错误 |
| Admin 初始业务首屏 | PASS | 标题、面包屑、CMS 正文一致 |
| Admin 技术/业务切换 | PASS | 技术落租户管理，切回业务落 CMS |
| Admin 业务“组织与成员”入口 | PASS | 已增加 |
| Admin 业务/技术导航隔离 | FAIL | `overview` 仍在业务模式，技术分区仍不一致 |
| Admin 邀请弹窗合同 | FAIL | 新增未授权邀请/预授角色，且缺少精确授权 Context |
| Admin console error/warn | PASS | 所测路径未发现运行时错误 |
| 基础可访问性 | FAIL | 非语义交互和无标签控件未收敛 |

测试方式：通过本地 HTTP 服务加载两份 HTML，使用浏览器执行真实点击、表单填写、模式切换、可见性与 console 检查。测试表单仅使用本地虚构数据，不触发外部服务或持久化操作。

## 8. 第六轮最低通过条件

1. 删除或隔离未获 Human Design Gate 授权的“管理员身份邀请 + 预授角色”链路；邀请中心恢复为当前三类赛事邀请语义。
2. 删除 Tenant 创建流程中的 `UPDATE bootstrap_state`。
3. 普通登录与注册登录调用同一个认证状态函数，得到完全一致的 Header、私有 Tab 和工作台状态。
4. Web Header、侧栏、工作台和目标页面统一消费服务端投影 mock；学生视角不可见、不可打开 reviewer/orgadmin 数据页。
5. Admin 业务模式移除技术治理 overview；技术模式按当前 `overview/platform/plugins` 分区组织。
6. 提交“本报告条目 → 修改文件/行 → 浏览器验证结果”对照表，防止修复一个入口而遗漏另一个入口。

P3 可继续作为质量轨推进，不单独阻断下一轮架构一致性批准；但新增表单至少应补齐 label、button type 和 modal 键盘焦点闭环。

## 9. 最终判定

第五轮对第四轮问题进行了有效修复，特别是历史授权 fallback、精确 Command 名称、未登录私有 Tab、正常登录和 Admin 首屏均已达到预期。然而，本轮又引入了比剩余文档误差更严重的新问题：把一个尚未授权、尚未实现且缺乏精确 Context 的管理员邀请流程定义为高权限 RoleAssignment 的生效路径。

因此本轮结论为：

> **REJECTED_DUE_TO_SCOPE_AND_AUTHORIZATION_REGRESSION — 暂不冻结。先移除未授权邀请授权链，再关闭 Tenant 流程和 Web Session/能力投影问题。**

审查过程中未修改四份受审文件，也未修改当前项目源代码；仅生成本报告。
