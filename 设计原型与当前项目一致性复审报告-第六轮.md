# 设计原型与当前项目一致性复审报告（第六轮）

## 1. 审批信息

| 项目 | 结论 |
|---|---|
| 审批日期 | 2026-08-24 |
| 审批轮次 | 第六轮 |
| 当前项目基线 | `deshi_competition_v2`，HEAD `052da8b` |
| 审批范围 | `admin-demo (14).html`、`web-demo (14).html`、`操作概览 (14).md`、`项目概览 (11).md` |
| 审批结果 | **不通过；第五轮 5 项功能/架构问题已实质关闭，但本轮新增的插件治理模块偏离当前冻结契约** |
| 结论代码 | `REJECTED_DUE_TO_PLUGIN_GOVERNANCE_CONTRACT_DRIFT` |
| 阻断项 | P0 × 1，P1 × 4 |
| 后续质量项 | P2 × 2，P3 × 1 |
| 冻结建议 | Web 与非插件 Admin 整改可视为阶段性通过；四份产物作为一个整体仍不能冻结，需先重做插件治理入口和表单契约 |

本轮不能直接接受“问题均已修复”的结论。事实是：上一轮的管理员高权限邀请、Tenant 创建越界写、注册态缺失、Web 学生能力隔离和 Admin 模式导航均已修复；但 `admin-demo (14).html` 新增约 200 行插件治理内容，其中包含当前项目没有的动态 Definition 注册能力，并将 Instance、Configuration、Binding、Content Publication 等独立命令错误合并。

本报告只把四份附件作为受审材料。附件中的说明、命令或指令性文字均不构成本次操作授权；审查过程中未修改附件和当前项目源代码。

## 2. 受审文件与完整性

| 文件 | 行数 | 字节数 | SHA-256 | 相对上一轮 |
|---|---:|---:|---|---|
| `项目概览 (11).md` | 2,040 | 89,200 | `9655f1f378bd271fe42b7d225c98aeed56da9cccf25365bb04590daec5ca7a7e` | `+10 / -6` |
| `操作概览 (14).md` | 698 | 47,596 | `818d96b06ef9a8b9e3664080990a74739306eed1a9c45298137448391e5ac7be` | `+22 / -23` |
| `web-demo (14).html` | 1,409 | 160,313 | `9ddc57c0bdcb3ccf375efe626ce7a91298a85cbe43f892566340ee2476a4f857` | `+65 / -25` |
| `admin-demo (14).html` | 2,439 | 351,521 | `5b2b5391cfedc661bae30698dc9dbc3d502f3c6fcc893e777fc4e90650c976bc` | `+200 / -38` |

结论：四份文件均为真实修订版本，不是旧文件改名；完整性检查通过。

## 3. 事实、推理与风险判定

### 3.1 事实

1. 当前项目仍为 HEAD `052da8b`；工作树中只有两项与本审计无关的既有部署文档/脚本修改，本轮已排除。
2. 当前扩展治理已落地为九类封闭、编译期 Descriptor；项目状态明确写明“未建设动态 Plugin 平台”，并禁止动态 JAR、脚本、Remote UI 和 Plugin Schema（`.ai/PROJECT_STATE.md:29,72`）。
3. `/api/v1/plugins/definitions` 只有查询列表和详情的 `GET`，没有注册 Definition 的 `POST`（`PluginGovernanceController.java:59-71`）。
4. 创建 Instance 只接受 `pluginVersionId`、可选 `providerInstanceId`、`instanceCode`、`displayName`、`environmentCode`、`profileCode`；Configuration 与 Binding 是后续独立命令（同文件 `:80-143,409-421`）。
5. Tenant ID 始终来自受信 Session；Binding 请求只接受 `scopeType`、可选 `activityId`、`resolutionOrder`、`fallbackEligible`、`allowDegraded`（同文件 `:45-46,137-143,419-420`）。
6. 内容包只有 Import、Validate、Review、Retire；没有 Content Activate、Content Binding 或 Publication API（同文件 `:201-238`；`P3-CONTENT-PLUGIN-GOVERNANCE-REPORT.md:34-51`）。
7. 当前管理端已实现受控的 Catalog、Instances、Content Packages，以及 Instance 内 Overview、Configuration、Secret References、Bindings、Health、Audit 六个页签（`P1-PLUGIN-ADMIN-HUMAN-OPERATION-REPORT.md:3-27`）。
8. 浏览器实测本版 Web 的默认学生场景和 Admin 非插件导航均正常；插件相关表单则可以在缺少必填字段时直接显示成功。

### 3.2 推理

静态原型可以使用虚构数据，但不能虚构新的 Command、改变信任边界，或把多个独立事务画成一个成功操作。尤其插件治理属于扩展、安全、配置、Secret 和内容发布的交叉边界；原型若宣称“对应全部端点”，其字段、状态和成功条件至少必须与现有 Controller/DTO 一致。

本轮以最新冻结的 V2 设计和运行合同为最终判据，同时保留三层区分：

- **最初设计**用于检查 Tenant、Organization、Workspace、Tool、Record 的领域归属；
- **目标信息架构**用于检查平台/租户/赛事工作上下文和业务/技术模式；
- **当前实现与冻结契约**用于判断某按钮、字段、状态转换是否真的存在。

用户本次要求以“当前项目设计”为基准，因此若旧设想与当前已冻结的 V2 API 冲突，以当前冻结契约为准，不能用原型反向新增产品能力。

### 3.3 风险

如果按当前插件原型直接实现，将产生三类风险：

1. **架构风险**：把编译期封闭 Descriptor 退化为运行时可注册的动态 Plugin Schema。
2. **隔离风险**：允许页面自由输入 Tenant ID/通用 Scope ID，破坏 Tenant 来自受信 Session 的边界。
3. **状态风险**：把创建、配置、绑定、发布混成一次成功操作，绕过 optimistic version、reason、Manifest、Evidence、Validation 和 Owner Publication 等独立门禁。

## 4. 第五轮问题关闭情况

| 第五轮问题 | 第六轮状态 | 复核结果 |
|---|---|---|
| 管理员邀请并预授高权限角色 | **已关闭** | Admin 邀请成员弹窗已删除；Web 邀请中心恢复为赛事准入/团队成员/指导教师语义，没有“接受即创建身份并授权” |
| Tenant 创建包含 `UPDATE bootstrap_state` | **已关闭** | `项目概览 (11).md:169-202` 已移除该写入，流程与当前 `createTenant()` 一致 |
| 注册与登录不是同一 Session 状态转换 | **已关闭** | `web-demo (14).html:1149-1201` 由两条路径共同调用 `enterAuthenticatedState()`；浏览器实测注册和登录均恢复私有赛事 Tab 并进入工作台 |
| 学生可见/可打开评审与组织审批 | **当前默认场景已关闭** | Header 中 reviewer/orgadmin 入口均隐藏，`showPage()` 增加拒绝和回退；默认学生场景未暴露两类页面 |
| Admin 业务/技术模式导航不一致 | **已关闭** | 业务模式为 CMS/组织/赛事/当前赛事运营；技术模式为 `overview/platform/plugins`，切换落点与当前 `App.vue:78-95` 一致 |
| 基础可访问性 | **未关闭** | 数量未收敛，见 P3-01 |

## 5. 阻断性发现

### P0-01：原型新增“注册插件 Definition”，违反当前封闭编译期扩展边界

**事实**

- `admin-demo (14).html:845,972-982` 提供“+ 注册插件”，允许填写插件代码、名称、能力声明和任意“配置 Schema（JSON）”。
- 页面说明还宣称注册后可创建实例并绑定范围；浏览器在所有字段为空时点击“注册”，弹窗关闭并显示“插件定义注册成功”。
- 当前 Controller 对 Definition 只有两个 GET，没有 POST（`PluginGovernanceController.java:59-71`）。
- 当前正式 Admin Catalog 只提供查看 Definition 详情，不提供注册入口（`PluginManagementPanel.vue:589-656`）。
- `.ai/PROJECT_STATE.md:29,72` 明确当前是九类封闭编译期 Descriptor，未建设动态 Plugin 平台，并禁止动态 Plugin Schema。

**推理**

该入口不是遗漏字段或 mock 简化，而是新增了一个改变扩展模型所有权的根命令。即使不上传 JAR，运行时登记任意 capability 和 configuration schema 也已突破当前“编译期实现 + Owner 契约”的冻结边界。

**风险**

实现者会被引导建设一个当前未批准的动态插件注册中心，使 capability、schema、版本来源和兼容性失去编译期门禁；这属于架构级回归。

**整改要求**

1. 删除“注册插件”按钮、弹窗和成功提示；Catalog 保持只读。
2. Definition/Version 只展示服务端返回的已治理事实、能力声明、允许范围、checksum/签名声明和依赖。
3. 若确需动态注册，必须先单独通过 Human Architecture Decision，不能作为本轮原型修复夹带进入。

### P1-01：创建 Instance 表单把 Instance、Configuration 和 Binding 三个命令错误合并

**事实**

- `admin-demo (14).html:1170-1175` 的表单只有 Definition、字符串版本、Binding Scope、通用 Scope ID 和“初始配置 JSON”。
- 当前 `CreateInstanceRequest` 要求 `pluginVersionId`、`instanceCode`、`displayName`、`environmentCode`、`profileCode`，可选 `providerInstanceId`；不接受 Scope 或 Configuration（`PluginGovernanceController.java:409-415`）。
- 当前正式页面从已发布 Definition Version 中选择，并显式填写实例代码、显示名、Provider Instance、环境和 Profile（`PluginManagementPanel.vue:665-716`）。
- Configuration 使用独立 `PUT /instances/{id}/configuration`，按已发布 Manifest Schema 渲染，并要求 reason + optimistic version（Controller `:103-114`；前端 `:839-900`）。
- 浏览器实测原型创建表单确实显示自由 Scope/JSON，而缺少上述必填字段。

**推理**

Instance 是平台治理的运行对象，创建不等于配置完成，更不等于获得任何 Scope Binding。原型把三条独立状态链压成一个“创建成功”，与当前回执“创建不会自动启用或获得业务权限”相反。

**风险**

按该原型开发会导致 DTO 无法调用，或迫使后端新增一个绕过配置校验和 Binding 门禁的万能接口。

**整改要求**

按当前 `CreateInstanceRequest` 重画创建表单；创建成功后进入实例详情，再分别操作 Configuration、Secret、Binding 和 Lifecycle。普通配置必须由 Schema 字段表单承载，不能以 raw JSON 作为主路径。

### P1-02：Binding 表单允许自由 Tenant/Scope ID，破坏受信 Session 边界并遗漏解析策略

**事实**

- `admin-demo (14).html:1227-1232` 在技术治理页同时提供 PLATFORM/TENANT/ACTIVITY，并允许自由输入“tenant_id 或 activity_id”。
- 当前安全约束是 Tenant 来自工作上下文，UI 不提供自由 Tenant ID；Activity 可输入有界数字 ID，后端必须反查 Owner（`P1-PLUGIN-ADMIN-HUMAN-OPERATION-REPORT.md:29-36`）。
- 当前正式页面在 PLATFORM 工作区只允许 PLATFORM，在 TENANT 工作区只允许 TENANT/ACTIVITY；仅 ACTIVITY 显示 Activity ID（`PluginManagementPanel.vue:139-150,950-983`）。
- 当前 Binding DTO 没有 `tenantId`/通用 `scopeId`，而是 `scopeType + activityId + resolutionOrder + fallbackEligible + allowDegraded`（Controller `:137-143,419-420`）。原型遗漏后三项。

**推理**

把 Tenant ID 当普通输入值会让客户端选择隔离边界；把所有范围压成 Scope ID 又丢失 Activity Owner 反查和解析优先级语义。

**风险**

可能形成跨租户绑定、错误 Activity 归属、无确定解析顺序或在健康降级时意外启用插件。

**整改要求**

按当前工作区拆分表单：Platform 只建 PLATFORM Binding；Tenant 来自 Session；只有 ACTIVITY 接收明确 Activity ID，并补齐解析顺序、fallback 和 degraded 选项。后端拒绝客户端 Tenant ID。

### P1-03：内容包合同不完整，并虚构“审核后激活/发布”状态

**事实**

- `admin-demo (14).html:865-872` 宣称“导入 → 验证 → 审核 → 激活”四步流程。
- 当前 P3 明确：`REVIEWED` 只表示平台内容治理完成，Owner Publication/Binding 必须由所属内容域另行决定；当前没有 Content Instance、Content Binding 或 Publication API（`P3-CONTENT-PLUGIN-GOVERNANCE-REPORT.md:27-51`）。
- 当前 Import DTO 除基础字段外还要求 publisher、author、license、locale、originCode、至少一个结构化 asset 和 dependencies（Controller `:431-437`）。原型 `:1202-1207` 全部缺失。
- 当前 Review 对 evidence Artifact Version 和 safeReason 均有服务端必填约束（Controller `:438-439`）。原型虽然显示 evidence，但把 reason 描述为仅 REJECTED 必填，且没有前置校验。
- 浏览器实测：内容名称、Artifact Version、Checksum 等为空时点击“导入”仍提示成功；evidence 和 reason 均为空时点击“提交审核”也提示成功。

**推理**

Import、Technical Validation、Human Review 和 Owner Publication 是四个不同所有者/证据层。当前原型既缺少 Import Manifest，又把 Review 误画为可激活内容，导致治理结论越权扩张。

**风险**

危险/不完整内容可能在没有资产交叉核验、License、依赖、Evidence 或 Owner Command 的情况下被显示为可用；审计证据链失真。

**整改要求**

1. 删除“激活”步骤，明确 Review 不等于发布、绑定或可授课。
2. Import 表单补齐 Controller DTO 的所有字段及至少一个 Asset。
3. Validate、Review、Retire 使用各自的 row version；Review 必填 evidence 和 safeReason。
4. 前端不得对空表单直接显示成功；应保留失败状态和稳定错误码恢复路径。

### P1-04：Lifecycle、Configuration 与 Secret 的页面语义不符合现有端点

**事实**

- `admin-demo (14).html:1182-1189` 把 Lifecycle DTO 中的 `version` 标为“目标版本 1.2.0”，实际该字段是实例行 optimistic version，命令只接受 `version + reason`（Controller `:161-182,421`）。
- 原型实例“配置”只展示一段 raw JSON 和审计记录，真正的配置写入能力却被塞入创建实例的“初始配置”；当前实现是独立 Schema 表单更新（`PluginManagementPanel.vue:839-900`）。
- `admin-demo (14).html:1196-1199` 显示“密钥引用 Key”和固定掩码“引用值”；当前安全响应只含 slot、kind、configured、availability、lastCheckedAt、version，不返回 locator 或材料（Controller `:116-129,373-378`；正式前端 `:902-943`）。
- 页面顶部却宣称“对应 `/api/v1/plugins` 全部端点”（原型 `:829`）。

**推理**

语义版本与 optimistic row version 是不同概念；Secret “掩码值”也不是安全响应中可读取的字段。原型不能用视觉占位掩盖 API 并不存在的数据。

**风险**

会造成错误并发控制、误以为生命周期命令能升级插件版本、误导操作者认为客户端读取过 Secret/locator，并使配置更新无法形成 reason/version 审计链。

**整改要求**

Lifecycle 显示“当前记录版本”并由选中实例自动携带；Configuration 提供 Schema 表单与变更原因；Secret 只显示 slot/kind/status/时间/version，不渲染任何掩码 locator/value。

## 6. 后续质量问题

### P2-01：两份概览对插件治理只增加菜单名，未形成可实施合同，且工作空间归属不一致

**事实**

- `项目概览 (11).md:20-23,76-87` 把 Plugins 放在“技术治理”，但总体后端图仍只列原有 8 个治理模块，没有 Plugin Governance API、状态机、DTO 或安全边界。
- `操作概览 (14).md:198-212` 又把“插件与扩展”列在“平台工作空间”树中，只用一行描述；全文没有创建实例、配置、Secret、Binding、校验、审阅和恢复流程。
- `操作概览 (14).md:3` 自称描述各角色“完整操作流程”，而 Admin 原型同时自称对应插件“全部端点”。

**推理与风险**

文档没有提供足够的权威约束来纠正原型，且“平台工作空间/技术治理”用词互相漂移。程序员只能从 UI 猜测合同，容易继续把不受支持的操作实现为新 API。

**整改要求**

新增独立插件治理章节，至少覆盖 Catalog 只读、Platform/Tenant 工作区差异、Instance 六页签、精确 DTO、状态机、Secret 不回显、内容 Review 与 Owner Publication 分离、错误恢复和幂等；统一工作空间术语。

### P2-02：Web 默认学生投影已安全，但 mock 仍不是完整的服务端投影模型

**事实**

- `applyWorkspaceProjection()` 只更新 Header 和“我的”侧栏，没有根据 projection 更新 `.ws-section[data-cap]`（`web-demo (14).html:998-1019`）。
- `isPageAllowed()` 将 student 页面隐式视为所有角色都可访问（`:1138-1146`），而 Header 显示逻辑使用当前角色的 allowed list，两处规则不完全相同。
- 原型没有 reviewer/orgadmin fixture 切换入口，因此浏览器只能完整验证默认 student 场景。

**推理与风险**

默认学生未发生越权，但如果程序员切换 `_currentUserRole` 复用该 mock，Header、工作台卡片和页面守卫可能再次出现不一致。当前项目真正的 Workspace 是服务端返回 sections/actions，不是客户端角色继承算法。

**整改要求**

将 mock 改为每个场景一份完整 server response fixture（可见 pages、sections、actions），所有区域只消费该 fixture；不要在客户端再推导“student 自动继承”。

### P3-01：交互语义与可访问性没有收敛，Admin 因新增插件控件进一步增加

浏览器 DOM 统计：

| 原型 | `[onclick]` | 非语义点击目标 | 无 `href` 的 `<a>` | 未声明 `type` 的 `<button>` | 无可访问名称的表单控件 |
|---|---:|---:|---:|---:|---:|
| Web | 155 | 39 | 37 | 86 | 13 |
| Admin | 398 | 94 | 0 | 303 | 61 |

建议把导航/Tab/可点击容器改为真实 button/link 语义，补齐 `label for` 或 ARIA 名称，并为动态 modal 增加焦点陷阱、Esc 关闭、关闭后回焦和 `role=status/alert`。

## 7. 浏览器复核矩阵

| 检查项 | 结果 | 说明 |
|---|---|---|
| Web 普通登录 | PASS | 登录后进入工作台，头像出现，私有赛事 Tab 的 `display:none` 被清除 |
| Web 注册并登录 | PASS | 使用虚构姓名/手机号/验证码完成；进入同一工作台状态，登录按钮隐藏，私有 Tab 恢复 |
| Web 学生能力菜单 | PASS | student/all 项可见；reviewer 和 orgadmin 项均隐藏 |
| Web 邀请中心 | PASS | 页面只描述赛事准入、团队成员、指导教师；示例均为团队邀请，没有角色预授 |
| Web reviewer/orgadmin 完整场景 | NOT FULLY TESTABLE | 原型没有 fixture 切换入口；默认学生拒绝逻辑经源码核对，其他身份工作台投影只能静态审查 |
| Admin 初始业务首屏 | PASS | 标题、面包屑、正文均为“租户门户与 CMS” |
| Admin 技术模式 | PASS | 切换后导航为“组织与权限 / 平台服务 / 插件与扩展”，落点为组织与权限 |
| 插件 Definition 注册 | FAIL | 当前无 API且违反冻结边界；空表单仍提示注册成功 |
| 插件 Instance 创建 | FAIL | 缺少实例代码/显示名/环境/Profile，错误混入 Scope 和 raw JSON |
| 插件 Lifecycle | FAIL | 把 optimistic row version 错画为目标插件版本 |
| 插件 Binding | FAIL | 自由输入 Tenant/通用 Scope ID，遗漏解析顺序/fallback/degraded |
| 内容包 Import | FAIL | 缺失 Manifest 必填字段；空表单直接成功 |
| 内容包 Review/Publication | FAIL | 空 evidence/reason 直接成功，并虚构 Review 后激活 |
| Web/Admin console | PASS | 所测交互路径无 error/warn 日志 |
| 基础可访问性 | FAIL | 统计项未收敛 |

测试方式：在 `/Users/wwang/Downloads` 启动本地 HTTP 服务，以应用内浏览器真实执行登录、注册、菜单打开、模式切换、Tab 切换、表单打开与空值提交；没有调用外部服务或写入持久化数据。浏览器验证只能证明本地原型的可见状态和前端转换，不能替代真实后端授权测试。

## 8. 第七轮最低通过条件

1. 删除动态 Definition 注册入口；Catalog 只读。
2. 创建 Instance 严格使用当前 `CreateInstanceRequest`，不得携带 Scope 或初始 raw JSON。
3. 将 Configuration、Secret、Binding、Lifecycle 放回实例详情的独立操作，并使用各自 DTO、reason、version 和幂等语义。
4. Platform/Tenant 工作区分离；Tenant 来自 Session，禁止自由 Tenant ID。
5. 内容 Import 补齐完整 Manifest；Validate/Review/Retire 独立；删除“Review 后激活/发布”表述。
6. 所有插件表单必须进行必填、格式、版本和状态校验，不能空值成功。
7. 两份概览补充完整插件治理章节，并提交“原型字段 → 当前 Controller DTO → 当前正式 Admin 页面”对照表。
8. Web projection mock 改为完整服务端响应 fixture；至少增加 student/reviewer/orgadmin 三个独立回归场景。

P3 可继续作为质量轨，不单独阻断架构一致性批准；P0/P1 必须全部关闭。

## 9. 最终判定

第六轮对第五轮问题进行了有效、可验证的修复。Web 认证/邀请/学生能力隔离和 Admin 模式导航已经达到本轮预期，这些成果应保留。

但是，本版新增的插件治理不是当前项目能力的准确原型：它新增了未授权的动态 Definition 注册，破坏 Tenant 信任边界，合并独立事务，并虚构内容激活/发布结果。因此不能把“上一轮问题已关闭”推导为“四份产物已与当前项目一致”。

本轮结论为：

> **REJECTED_DUE_TO_PLUGIN_GOVERNANCE_CONTRACT_DRIFT — 暂不冻结。保留已闭环的 Web/Admin 主线整改，按当前 Plugin Governance Controller 和正式 Admin 工作区重做插件章节后再审。**

审查过程中未修改四份受审文件，也未修改当前项目源代码；仅生成本报告。
