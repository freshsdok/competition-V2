# 签到概览 Phase 2 开发任务清单

## Phase 2A：数据库和后端统计基础

目标：

- 落地已确认的签到统计口径。
- 新增必要索引 SQL。
- 新增后端 Query/VO/常量/角色归一化工具。
- 实现核心聚合 Mapper。

修改文件范围：

- `db/migration/*checkin_overview*.sql`
- `CompetitionSceneCheckinOverview*` 后端类
- `CompetitionSceneCheckinOverviewMapper.xml`

前置条件：

- 已确认页面签到采用赛场级签到口径。
- 已确认状态事实源使用 `SCHEDULE + REPORT + DONE`。
- 已确认 `COMPETITION/REPORT` 全部排除，`REPORT_SIGN` 流水仅作审计和最后签到时间辅助。
- 已确认 `选手/PLAYER` 按队员处理，当前无队长记录且首期去除队长相关统计。

测试方式：

- Mapper 聚合测试。
- 角色归一化单元测试。
- EXPLAIN 检查索引命中。

验收标准：

- 能按筛选条件返回总人数、已签到、未签到、赛场数。
- 不出现 N+1 查询。
- 取消、重复、扫码未确认不计入已签到。
- 只统计有效、已匹配的队员赛场目标。

风险点：

- 历史角色值混用，需要角色归一化测试覆盖 `队员/MEMBER/选手/PLAYER` 和排除角色。

## Phase 2B：概览接口与自动化测试

目标：

- 完成统计、赛场分页、详情、人员明细接口。
- 补充权限注解和返回结构。

修改文件范围：

- Controller/Service/Mapper
- DTO/VO
- 后端测试

前置条件：

- Phase 2A 聚合 SQL 稳定。

测试方式：

- 接口级测试。
- 空数据、全签、部分签、全部未签、取消场景。

验收标准：

- 接口返回结构与设计一致。
- 分页接口不返回人员大明细。
- 详情接口按团队分组正确。

风险点：

- 历史数据角色不规范导致统计与人工预期不一致。

## Phase 2C：前端概览页面与图表

目标：

- 新增菜单目标页面。
- 完成筛选、统计卡片、三个图表、赛场卡片列表。

修改文件范围：

- `old-code-admin/src/api/tournament/checkinOverview.js`
- `old-code-admin/src/views/tournament/checkinOverview/**`

前置条件：

- Phase 2B 接口可用。

测试方式：

- 前端构建。
- PC 浏览器手工验证。
- 图表空数据和刷新验证。

验收标准：

- 页面无新增大型依赖。
- 图表复用实例并正确 resize。
- 不逐赛场请求详情。

风险点：

- 现有 ECharts 示例未清理 resize listener，新页面需避免重复监听泄漏。

## Phase 2D：赛场详情与团队人员视图

目标：

- 完成赛场详情页/抽屉。
- 团队统计和人员签到明细可筛选。

修改文件范围：

- 详情组件
- 人员列表组件
- API 调用

前置条件：

- 人员明细接口稳定。

测试方式：

- 多团队、多同名人员、无团队人员验证。

验收标准：

- 默认按团队分组。
- 不使用姓名作为唯一 key。
- H5 下不出现必须横向滚动才能阅读的宽表格。

风险点：

- 部分历史目标 `target_type` 为空，前端不能依赖该字段展示身份。

## Phase 2E：H5适配、联调和性能验证

目标：

- 完成管理端页面响应式适配。
- 完成自动刷新、页面不可见暂停、重复请求抑制。
- 性能压测和 EXPLAIN 复核。

修改文件范围：

- 前端样式与 composable
- 后端 SQL 优化

前置条件：

- Phase 2C/2D 功能完成。

测试方式：

- 375px、768px、1440px 视口验证。
- 30 秒刷新长时间运行检查。
- 数据量模拟或真实库 EXPLAIN。

验收标准：

- H5 单列卡片可读。
- 筛选抽屉可用。
- 页面不可见不轮询。
- 上一次请求未完成时不发起下一次刷新。

风险点：

- 真实库 `competition_apply_info` 大，禁止刷新时回表大范围扫描。

## Phase 2F：菜单SQL、部署说明和验收

目标：

- 输出幂等菜单 SQL。
- 输出部署说明和验收清单。
- 完成最终构建验证。

修改文件范围：

- `db/migration/*checkin_overview_menu.sql`
- 发布说明文档

前置条件：

- 前后端功能联调完成。

测试方式：

- 后端 compile。
- 前端 build。
- 菜单权限账号验证。

验收标准：

- `赛事管理 -> 签到概览` 可见。
- 有权限可访问，无权限不可访问。
- 不影响现有现场安排、扫码、证件核验权限。

风险点：

- 角色菜单授权范围需要业务确认是否只给 admin，还是沿用赛事管理已有角色。
