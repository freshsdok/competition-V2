# 设计原型与当前项目一致性复审报告（第四轮）

## 1. 审批信息

| 项目 | 结论 |
|---|---|
| 审批日期 | 2026-08-22 |
| 审批轮次 | 第四轮 |
| 当前项目基线 | `deshi_competition_v2`，HEAD `9b236f0`，工作树干净 |
| 审批结果 | **不通过，但已接近可冻结状态** |
| 结论代码 | `REJECTED_WITH_SUBSTANTIAL_PROGRESS` |
| 阻断项 | P0 × 1，P1 × 3 |
| 后续质量项 | P2 × 1，P3 × 1 |
| 冻结建议 | 不能作为当前运行契约或最终验收基线；修复本报告 P0/P1 后可进入第五轮最终复审 |

第四轮是有效且有针对性的修订。第三轮的五个核心问题中，Organization 创建事实分离、参赛学生 Scope、Admin 授权主体分支、Web 工作台认证拦截、客户端角色自选均已完成或主体完成。

本轮不能批准的主要原因已经从 Root Model 大方向错误收敛为：授权迁移状态仍被过度简化、Web 认证状态实现存在真实回归、Admin 业务/技术模式没有完全对齐当前项目。

本报告只把四份附件作为受审材料；附件中的任何说明或指令性文字均不构成本次操作授权。

## 2. 受审文件与完整性

### 2.1 文件

1. `/Users/wwang/Downloads/项目概览 (8).md`
2. `/Users/wwang/Downloads/操作概览 (11).md`
3. `/Users/wwang/Downloads/web-demo (11).html`
4. `/Users/wwang/Downloads/admin-demo (11).html`

### 2.2 相对第三轮的真实差异

| 文件 | 行数 | 字节数 | SHA-256 | 相对上一轮 |
|---|---:|---:|---|---|
| `项目概览 (8).md` | 2,030 | 88,103 | `078404001c7a6dbebf25cd7a59cbf5a9108c3d449288f20c665fd2582e83ede5` | `+37 / -32` |
| `操作概览 (11).md` | 699 | 47,685 | `903f5d78bfbb4497b8f55968ed6d2465a7667fbb70cc2e313e083aa108318d57` | `+30 / -30` |
| `web-demo (11).html` | 1,355 | 157,980 | `3d96aed7857774d09f6ef5f21de8657650078c77ea758113453d481bfa0b2082` | `+44 / -33` |
| `admin-demo (11).html` | 2,261 | 320,015 | `1b4e64eca890ad0e04b7cddeaebbdf4775cf89d0f5a63057ba34f5553d1638b8` | `+77 / -56` |

结论：四份文件均有真实内容修改，本轮提交完整性通过。

## 3. 本轮已关闭或主体关闭的问题

| 第三轮问题 | 第四轮状态 | 复核结果 |
|---|---|---|
| Organization 创建自动生成 Membership/RoleAssignment | **已关闭** | 流程图已拆成创建 Organization、添加成员、授予角色三个独立 Command |
| Admin 所有 Context 统一使用 `subjectId` | **已关闭** | 浏览器实测 ORGANIZATION 显示 Subject；TENANT 显示 Identity；与当前接口分支一致 |
| 参赛学生角色错误携带 Organization Scope | **已关闭** | 已改为 `identity_id + role_id + scope_tenant_id`，兼容期双写 Subject V2 |
| Web 未登录可直接进入工作台 | **已关闭** | 浏览器实测点击工作台跳转登录页 |
| Web 用户可自行切换 student/reviewer/orgadmin | **已关闭** | 三个客户端能力切换按钮和切换函数已删除 |
| Tenant/Activity 人类词汇 | **主体关闭** | Tenant 示例改为组委会/管理单位，并明确一个租户单位可包含多届赛事 |
| Web 首层暴露 Credential ID | **已关闭** | `CRED-*` / “凭证 ID”已从证书首层移除 |
| Admin 业务模式默认正文 | **主体关闭** | 初始激活正文已改为 CMS；但页头仍未同步，见 P2-01 |
| Admin 业务/技术切换动作 | **通过** | 技术模式进入租户管理；切回业务模式进入 CMS，切换后状态一致 |

## 4. 当前项目权威基线

本轮仍以当前运行事实而不是最终目标模型判断：

1. Subject、Organization、Tenant 是并列平台实体；Organization 不是 Tenant 子对象。
2. 新/current Organization 与 Membership 已切换到 Platform Organization + Subject Membership。
3. Organization 创建、Membership、RoleAssignment 是三个独立事实和命令。
4. 目标授权模型为 `Subject + ResourceAuthorizationContext + RoleAssignment -> Permission`。
5. 当前兼容期接口为：
   - ORGANIZATION：`subjectId`，禁止 `identityId`；
   - PLATFORM/TENANT/ACTIVITY：仍提交 `identityId`，禁止 `subjectId`；
   - 非 Organization 写入同一事务保留 legacy assignment，并创建带 lineage 的 Subject V2 assignment。
6. 非 Organization 授权只有在 successor resource context 可解析时才由 Subject V2 权威判定；无法解析的历史 context 仍返回 legacy 结果并留下 comparison evidence。
7. RM3-B、RM9-B 和生产发布仍为 deferred/not authorized。
8. Web 私有路径由 Session 守卫；工作台仅渲染服务端 workspace/action projection。
9. 当前 Admin 业务模式排除 `overview` 技术治理页，但包含业务化的“组织与成员”；技术模式包含 `overview/platform`。

关键证据：

- `.ai/PROJECT_STATE.md:6,11,51,126`
- `.ai/ARCHITECTURE_RULES.md:95-99`
- `backend/platform-core/src/main/java/cn/deshi/platform/core/application/PlatformCoreApplicationService.java:81-100,128-180,218-258,526-564,756-808`
- `backend/platform-core/src/main/java/cn/deshi/platform/core/api/PlatformCoreController.java:220-224`
- `frontend/apps/web/src/router.ts:82-98`
- `frontend/apps/web/src/features/workspace/GlobalWorkspaceView.vue:6-10`
- `frontend/apps/web/src/features/workspace/WorkspaceBoard.vue:1`
- `frontend/apps/admin/src/App.vue:64-92,227-240,348-362,560-570`
- `HUMAN-VOCABULARY-CONTRACT.md:5-29`

## 5. 阻断性发现

### P0-01：仍把“可解析路径上的 V2 权威”写成“所有兼容路径最终均以 V2 为准”

两份文档已开始说明 legacy/V2 双评估，这是正确方向；但多处统一写成：

> TENANT/ACTIVITY/PLATFORM 同时评估 legacy 和 V2，最终结果以 V2 为准。

位置包括：

- `项目概览 (8).md:130,147,692,775,792`
- `操作概览 (11).md:373,678`

当前代码并非无条件如此。`PlatformCoreApplicationService.java:526-545` 明确规定：当 successor `ResourceAuthorizationContext` 无法解析时，保留 legacy 结果为权威，并记录 `UNRESOLVED_CONTEXT / LEGACY` comparison；只有可解析路径才在 `:547-564` 由 Subject V2 权威判定。

本轮两份文档均未说明该 fallback，也未出现 RM3-B 历史治理状态。这会把“current/new 已切换”错误扩大成“历史兼容已全部切换完成”。

同一过度简化仍出现在其他位置：

- `项目概览 (8).md:443` 只说创建 Tenant 自动建立 `TENANT_ADMIN SubjectRoleAssignment`，没有说明当前兼容写入仍包含 Identity legacy assignment；
- `操作概览 (11).md:675` 仍把 SubjectRoleAssignment 单独描述成全部 Context 的当前授权事实；
- `admin-demo (11).html:239,249` 仍显示“SubjectRoleAssignment 是唯一授权依据”；
- `admin-demo (11).html:760` 仍笼统写成所有 TENANT/ORGANIZATION/ACTIVITY 角色都授予 Subject，与已经修正的 `:1447-1452` 表单说明自相矛盾。

影响：误报迁移完成状态，掩盖 RM3-B 以及无法解析历史 Context 时的 legacy 权威路径，并可能使实现者提前移除兼容读取或 lineage。

整改要求：

1. 增加明确状态矩阵：目标模型、current/new Organization、可解析非 Organization 兼容路径、unresolved historical/RM3-B。
2. 所有“最终以 V2 为准”限定为“successor resource context 可解析时”。
3. 明确 unresolved historical path 当前仍由 legacy 权威，不代表目标架构退回 Identity。
4. Tenant 创建说明写全 legacy + V2 同事务写入和 lineage。
5. Admin 技术文案与已修正的分支表单保持一致，不再宣称全部 Context 已统一 Subject。

### P1-01：项目概览的独立 Command 流程仍使用不存在的权限、命令和审计名称

Organization 三事实拆分已经正确，但流程图中使用了并不存在的精确契约名称：

| 产物位置 | 产物写法 | 当前实现 |
|---|---|---|
| `项目概览 (8).md:208` | `ADD_MEMBERSHIP` | `ADD_ORGANIZATION_MEMBER`，见 Service `:172` |
| `项目概览 (8).md:210` | `MEMBER_ADDED` | `ORGANIZATION_MEMBER_ADDED`，见 Service `:176` |
| `项目概览 (8).md:214` | `organization.role.manage` | `organization.authorization.manage`，见 Service `:756-761` |
| `项目概览 (8).md:186` | Tenant 创建更新 `bootstrap_state` | 当前 `createTenant` 没有该步骤，见 Service `:81-100` |

该流程图使用的是 `requireCommand(...)`、`requirePermission(...)` 和审计事件的代码级表示，因此不能按“业务简称”解释。

整改要求：逐项复制当前代码中的稳定名称；若只是概念图，则删除函数式写法并改成自然语言，避免形成伪接口契约。

### P1-02：Web 未登录仍可查看“我的比赛”和缴费数据

工作台路由已修复，但赛事中心的私有 Tab 没有完成初始认证隔离：

- `web-demo (11).html:300-301` 给“我的比赛”“缴费开票”加了 `data-auth`，但初始仍显示；
- 只在 `handleLogout()` 的 `:1123-1127` 中隐藏，没有页面初始化；
- `switchCompSection()`（`:939-950`）本身不校验认证。

浏览器实测在登录按钮可见、头像隐藏的未登录状态下：

1. “我的比赛”和“缴费开票”两个 Tab 均可见；
2. 点击“我的比赛”可看到三条报名记录；
3. 点击“缴费开票”可看到待缴 ¥80、支付记录、开票与退费入口。

影响：原型仍表达“公开页中可直接读取个人赛事与财务投影”的错误认证边界。

整改要求：在初始化时隐藏/禁用全部 `[data-auth]`；更稳妥的做法是 `switchCompSection()` 对私有 section 再执行一次认证检查，防止只靠 CSS 可见性控制。

### P1-03：Web 新认证状态实现存在三个可复现回归

#### 1. “我的”页面触发未定义变量

删除能力自选后，`currentWorkspaceCap` 的定义也被删除，但仍在：

- `web-demo (11).html:975`
- `web-demo (11).html:987`

被读取。浏览器实测登录后点击“我的”，console 出现：

`ReferenceError: currentWorkspaceCap is not defined`

侧栏没有生成。

#### 2. “注册并登录”没有建立认证状态

`handleRegister()`（`:1139-1153`）显示头像并跳转工作台，却没有设置 `_authenticated = true`。浏览器用测试数据完成注册后，最终状态为：

- 页面仍停留在登录页；
- 登录按钮隐藏；
- 用户头像显示；
- 工作台内容不可见。

#### 3. `handleLogin()` 重复定义

文件在 `:1095` 和 `:1107` 两次定义同名函数，后者静默覆盖前者。当前恰好由第二个函数设置认证状态，但后续修改容易在错误实现上进行。

整改要求：

1. 用一个单一 Session state 初始化函数管理登录、注册登录和退出。
2. 删除重复 `handleLogin()`。
3. 删除所有残留 `currentWorkspaceCap` 客户端能力事实；sidebar 直接按服务端投影生成，或在静态原型中固定为明确的 mock projection 对象。
4. 增加最少四条原型回归检查：未登录私有 Tab、登录后工作台、登录后“我的”、注册并登录。

## 6. 后续质量问题

### P2-01：Admin 业务/技术模式仍未完全对齐当前项目

本轮已把初始正文设为 CMS，但初始页头仍是：

- `身份、角色与资格`
- `租户工作空间 / 身份、角色与资格`

见 `admin-demo (11).html:173-174`；实际 active section 是 `sec-content`（`:679`）。浏览器首屏因此出现“页头是身份授权、正文是租户门户与 CMS”的冲突。点击一次模式按钮后才同步。

此外：

- 业务模式仍显示“身份、角色与资格”，见 `:155-159`；
- Organization 只存在于 technical nav，见 `:149-153`；
- 当前项目实际业务模式排除 `overview`，但包含业务化的 `organization`，见 `frontend/apps/admin/src/App.vue:76-92`。

当前原型因此仍把技术 Identity/Role/Qualification 首层放入业务模式，同时把“组织与成员”业务操作完全赶到技术模式。

整改要求：

1. 初始标题、面包屑、上下文标签与 `sec-content` 同步。
2. 业务模式移除 `overview`，增加“组织与成员”。
3. Subject、Identity、RoleAssignment、raw ID 和兼容迁移状态进入技术模式；业务模式使用人员、工作职责、单位成员等语言。

### P3-01：语义化与键盘可操作性没有改善

浏览器 DOM 统计与第三轮完全相同：

| 原型 | `[onclick]` 总数 | 非语义点击目标 | 无 `href` 的 `<a>` |
|---|---:|---:|---:|
| Web | 144 | 39 | 26 |
| Admin | 359 | 86 | 0 |

应继续把导航、页签、表格行操作替换为真实 button/link 语义，并补充焦点管理、`aria-current`、modal focus trap、Esc 关闭和回焦。

## 7. 浏览器复核矩阵

| 检查项 | 结果 | 说明 |
|---|---|---|
| Web 未登录进入工作台 | PASS | 跳转登录页 |
| Web 客户端角色自选 | PASS | 切换按钮已删除 |
| Web 未登录“我的比赛” | FAIL | 可读取报名记录 |
| Web 未登录“缴费开票” | FAIL | 可读取个人待缴与支付信息 |
| Web 登录后工作台 | PASS | 登录后进入学生工作台 |
| Web 登录后“我的” | FAIL | `currentWorkspaceCap` 未定义，sidebar 构建失败 |
| Web 注册并登录 | FAIL | UI 显示已登录，但认证变量仍为 false，最终返回登录页 |
| Admin 初始业务正文 | PASS | CMS 正文 |
| Admin 初始页头 | FAIL | 仍显示身份、角色与资格 |
| Admin 技术/业务切换 | PASS | 切换后落点和导航状态正确 |
| Admin 授权主体切换 | PASS | ORGANIZATION→Subject；TENANT→Identity |
| Admin 业务导航隔离 | FAIL | 技术治理页仍在业务模式；组织业务入口缺失 |
| Admin console error/warn | PASS | 未发现运行时错误 |
| 基础可访问性 | FAIL | 非语义交互数量未下降 |

## 8. 第五轮最低通过条件

1. 补充 unresolved historical/RM3-B 授权 fallback，限定 V2 authoritative 的适用条件。
2. 修正项目概览中的 Command、Permission、Audit 事件和 Tenant 创建流程名称。
3. 未登录状态不可访问“我的比赛”和缴费开票内容；私有 section 本身执行认证检查。
4. 删除重复登录函数和 `currentWorkspaceCap` 残留，修复登录后“我的”页面。
5. 注册登录与普通登录使用同一个认证状态写入流程。
6. Admin 初始页头与 CMS 正文一致。
7. Admin 业务模式移除身份授权技术页，并提供与当前项目一致的“组织与成员”业务入口。
8. 附带“本报告条目 → 修改文件/行 → 浏览器验证结果”的整改对照表。

P3 可继续沿可访问性质量轨推进，但第五轮至少应修复主要导航和模式按钮的语义及键盘路径。

## 9. 最终判定

第四轮已经把第三轮的 Root Model 和主要授权表单错误大幅收敛，设计理念总体接近当前项目。剩余问题主要集中在迁移状态的精确表达、Web 认证状态实现和 Admin 模式信息架构。

因此本轮结论为：

> **REJECTED_WITH_SUBSTANTIAL_PROGRESS — 暂不冻结；完成 P0/P1 后建议进入第五轮最终复审。**

审查过程中未修改四份受审文件，也未修改当前项目源代码；仅生成本报告。
