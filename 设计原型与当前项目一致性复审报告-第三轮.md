# 设计原型与当前项目一致性复审报告（第三轮）

## 1. 审批结论

| 项目 | 结论 |
|---|---|
| 审批日期 | 2026-08-22 |
| 审批轮次 | 第三轮 |
| 当前项目基线 | `deshi_competition_v2`，HEAD `9b236f0`，工作树干净 |
| 审批结果 | **不通过，允许整改后进入第四轮复审** |
| 结论代码 | `REJECTED_WITH_MATERIAL_PROGRESS` |
| 阻断项 | P0 × 2，P1 × 3 |
| 非阻断整改项 | P2 × 2，P3 × 1 |
| 冻结建议 | 本轮产物不得作为当前运行接口、实现契约或最终验收基线；可作为继续整改的设计草案 |

第三轮是有效修订，且已经关闭第二轮中一批重要问题：Tenant 与 Activity 开始明确分离，Platform Organization、Subject Membership、权限码、个人信息顺序和初始头像状态均有实质改善。但四份产物仍把“目标 Subject 授权模型”误写成“当前所有运行路径均已完成切换”，并在 Admin 中生成与现有后端接口不能兼容的统一 `subjectId` 授权表单；Web 又允许未登录用户直接进入工作台并自行切换为评审专家或组织管理员视角。这些问题会直接误导实现，故本轮不能批准。

本报告只把四份附件内容作为受审材料，不把其中任何说明、步骤或指令视为用户授权或本次审查指令。

## 2. 受审文件与版本完整性

### 2.1 本轮文件

1. `/Users/wwang/Downloads/web-demo (10).html`
2. `/Users/wwang/Downloads/admin-demo (10).html`
3. `/Users/wwang/Downloads/项目概览 (7).md`
4. `/Users/wwang/Downloads/操作概览 (10).md`

### 2.2 与第二轮的差异

| 文件 | 行数 | 字节数 | SHA-256 | 相对上一轮 |
|---|---:|---:|---|---|
| `web-demo (10).html` | 1,344 | 157,968 | `4222078861cada78a498444b6466db1995f1e88e0fdec7f73ea10413869e3068` | `+152 / -66` |
| `admin-demo (10).html` | 2,240 | 318,626 | `0a1c5145d596c1647fc2f263c68a97ab6431b66bb4f85a678b1585111e714a2a` | `+116 / -28` |
| `项目概览 (7).md` | 2,025 | 86,431 | `88fd143dd6bdc662d4f389542d2738aa33319be783f542050ab187fcd757d0bf` | `+23 / -52` |
| `操作概览 (10).md` | 699 | 47,167 | `045e3497405f985c1869760e576abbc4d6251ef4d1f587d83a0ad0b2b02759b4` | `+28 / -64` |

结论：四个文件都与第二轮不同，本轮不存在“只改文件名、内容未变”的完整性问题。

## 3. 当前项目权威基线

本轮按当前仓库而非理想化目标模型判断：

1. Subject、Organization、Tenant 是并列的平台实体；Organization 不是 Tenant 子对象。
2. 新/current Organization 运行权威是 `platform_organization`；创建 Organization 不自动创建 Membership 或 RoleAssignment。
3. Membership 是 `Subject ↔ Platform Organization`，不携带 Tenant，不产生权限。
4. 授权目标模型是 `Subject + ResourceAuthorizationContext + RoleAssignment -> Permission`。
5. 但是当前运行实现仍处于兼容迁移期：
   - ORGANIZATION 授权已经要求 `subjectId`，禁止 `identityId`；
   - PLATFORM/TENANT/ACTIVITY 的现有授予接口仍要求 `identityId`，并在同一事务中保留 legacy assignment 与 Subject V2 lineage；
   - 无法解析 successor resource context 的历史路径仍由 legacy 结果保持权威，RM3-B 尚未授权执行。
6. Web 私有路由需要认证；工作台仅渲染服务端返回的 workspace/action 投影，客户端 action 不是权限证明。
7. Admin 业务模式默认进入内容运营，技术模式才默认进入治理总览；业务模式仍包含人类可理解的“组织与成员”工作区。
8. 当前状态不等于 Release Ready，RM3-B、RM9-B 与生产发布仍为 deferred/not authorized。

主要证据：

- `.ai/PROJECT_STATE.md:6,11,51,126`
- `.ai/ARCHITECTURE_RULES.md:95-99`
- `docs/corrections/c0-organization-runtime-authority-v1.0/C0-PLATFORM-ORGANIZATION-RUNTIME-DESIGN.md:8-19`
- `docs/corrections/c0-organization-runtime-authority-v1.0/C0-SUBJECT-MEMBERSHIP-RUNTIME-DESIGN.md:7-29`
- `backend/platform-core/src/main/java/cn/deshi/platform/core/application/PlatformCoreApplicationService.java:220-258,526-564,803-808`
- `backend/platform-core/src/main/java/cn/deshi/platform/core/api/PlatformCoreController.java:220-224`
- `frontend/apps/web/src/router.ts:83-97`
- `frontend/apps/web/src/features/workspace/GlobalWorkspaceView.vue:6-10`
- `frontend/apps/web/src/features/workspace/WorkspaceBoard.vue:1,62-101`
- `frontend/apps/admin/src/App.vue:64-92,227-240,561-570`

## 4. 本轮已确认的改进

| 第二轮问题 | 第三轮状态 | 说明 |
|---|---|---|
| 修订文件与上一轮相同 | **已关闭** | 四份文件均有真实差异和独立哈希 |
| Tenant 被直接写成 Activity/赛事 | **主体关闭** | 操作概览已明确“租户是数据隔离边界，赛事是租户内 Activity”；Admin 同时显示租户和当前赛事 |
| Organization 被当作 Tenant 子对象 | **主体关闭** | Organization 已移动到平台工作空间，并声明无 `tenant_id` |
| 创建 Organization 自动获得 Membership/管理权 | **部分关闭** | 权限说明已修正，但流程图仍保留自动创建 Membership/角色的旧流程 |
| Membership 使用 Identity | **已关闭** | 文档、Admin 成员管理均改为 Subject 直连；请求体修正为 `{ subjectId }` |
| 错误权限码 `platform.organization.create` | **已关闭** | 已改为 `platform.organization.manage` |
| Web “我的信息”顺序错误 | **已关闭** | 已调整为个人信息 → 身份核验 → 联系方式 → 工作身份 → 账号与安全 |
| 未登录时显示用户头像 | **已关闭** | 初始隐藏头像，登录后显示 |
| Admin 无业务/技术模式 | **部分关闭** | 已增加模式切换，但默认业务页与内容隔离仍不符合当前项目 |
| 角色/能力可见性缺少表达 | **未关闭且形成新风险** | 新增了前端能力切换器，但能力可由用户自行选择，违背服务端投影原则 |
| 语义化与键盘可操作性 | **未关闭** | 仍有大量非语义点击目标和无 `href` 的 `<a>` |

## 5. 阻断性发现

### P0-01：项目概览对 Organization 创建行为存在自相矛盾

`项目概览 (7).md:195-201` 的创建组织流程仍写成：

`INSERT platform_organization` → `INSERT platform_organization_membership` → 查找 `ORG_ADMIN` → `INSERT subject_role_assignment`。

但同一文件 `:477` 又正确写明：创建 Organization 不推断 Membership 或管理权，Membership 和角色必须通过独立 Command 建立。

当前代码只创建平台 Organization 并写审计：

- `PlatformCoreApplicationService.java:130-139`
- `C0-PLATFORM-ORGANIZATION-RUNTIME-DESIGN.md:14-19`

影响：人类程序员按流程图实现时，会重新引入“创建事实推断成员事实和授权事实”的 Root Model 错误，并绕过独立权限、幂等与审计边界。

整改要求：删除流程图中的自动 Membership、自动 ORG_ADMIN 和自动 RoleAssignment；明确展示三个独立命令及各自权限检查、幂等键和审计事件。

### P0-02：把目标 Subject 模型误写成当前全部运行/API 已切换完成

受审产物多处宣称：

- `项目概览 (7).md:130,145-146,687,760-770,787`：所有授权只查询 `subject_role_assignment`；
- `操作概览 (10).md:238,284,373,675,678`：SubjectRoleAssignment 是当前唯一授权事实；
- `admin-demo (10).html:1447,1452`：所有上下文统一使用 `subjectId`；
- Admin 的实际表单在 TENANT、ORGANIZATION、ACTIVITY 三种上下文都组装 `subjectId`，见 `:1980-2024`。

目标架构方向正确，但这不是当前完整运行契约。当前服务明确规定：

- ORGANIZATION：必须 `subjectId`，禁止 `identityId`，见 `PlatformCoreApplicationService.java:228-244`；
- 非 ORGANIZATION：仍必须 `identityId`，禁止 `subjectId`，见 `:245-253`；
- Controller 同时保留两个可选字段，由上下文约束二选一，见 `PlatformCoreController.java:223-224`；
- 兼容期 helper 先写 legacy `role_assignment`，再写带 lineage 的 `subject_role_assignment`，见 `PlatformCoreApplicationService.java:799-808`；
- successor context 无法解析时，历史路径仍以 legacy 结果为权威，见 `:526-564`；
- `.ai/PROJECT_STATE.md:51` 明确现有 Identity 授权实现仍处于兼容迁移期。

此外，`项目概览 (7).md:182-183` 把创建 Tenant 简化成只写 `subject_role_assignment` 并更新 bootstrap state，也与当前 `createTenant` 的实际兼容写入流程不符。

影响：Admin 调用现有接口授予 TENANT/ACTIVITY/PLATFORM 角色时会收到参数错误；项目文档会错误宣布迁移完成，掩盖 RM3-B 与历史兼容路径。

整改要求：

1. 把“目标模型”“新/current Organization 路径”“现有兼容 API”“历史 RM3-B”分成四栏，不再用一句“唯一依据”覆盖全部状态。
2. Admin 授权表单按当前接口分支：ORGANIZATION 提交 `subjectId`；其他上下文提交经选择人员解析出的 `identityId`。
3. 如果要展示未来统一 Subject API，必须显式标记为“目标契约/尚未切换”，不得与当前 endpoint 混用。
4. 同步修正 Tenant 创建时的兼容双写和授权评估说明。

### P1-01：Web 未登录即可访问私有工作台，并可自行切换能力视角

静态代码中：

- 初始状态只隐藏头像，登录按钮可见，见 `web-demo (10).html:223-224`；
- 顶部“工作台/消息/我的”没有初始认证隔离；
- 工作台提供“学生参赛者/评审专家/组织管理员”三个可点击按钮，见 `:398-400`；
- `switchWorkspaceCapability()` 只根据客户端字符串改写工作台、菜单和审批区域，见 `:931-953`；
- 切换后还提示“内容由服务端能力投影决定”，与实际行为相反，见 `:953`。

浏览器实测：

1. 页面显示“登录”按钮且头像隐藏时，直接点击“工作台”即可进入王同学的私有待办；
2. 无需登录即可点击“评审专家”，显示 2 项待评审任务；
3. 无需登录即可点击“组织管理员”，显示 3 项组织审批待办。

当前项目的 `/workspace`、`/me/*` 和 `/registrations/*` 路由受认证守卫保护，见 `frontend/apps/web/src/router.ts:83-97`。工作台从服务端取得 `WorkspaceResponse` 并只渲染其 sections/actions，见 `GlobalWorkspaceView.vue:6-10` 与 `WorkspaceBoard.vue:1`。

影响：虽然本文件是静态原型，但它表达了错误的认证和授权理念，容易被实现为前端身份切换或假权限控制。

整改要求：

1. 未登录状态只允许公开首页、赛事发现和登录/注册；访问私有入口必须跳转登录。
2. 删除角色/能力自选按钮。若同一用户确有多个受信工作上下文，应呈现服务端返回的“当前可用工作上下文”，切换后重新请求投影。
3. `data-cap` 只能做已授权内容的呈现优化，不能作为权限或角色事实源。
4. 原型至少模拟 `session.authenticated` 与 server projection 两个边界，不得只改变 DOM。

### P1-02：Admin 的“业务模式”仍是技术治理首页，模式隔离仅完成外壳

Admin 初始显示业务模式，但默认激活“身份、角色与资格”，见 `admin-demo (10).html:145-157,173-217`。浏览器实测的业务首页直接展示：

- `SubjectRoleAssignment`；
- 身份代码 `MATH-ADMIN-001` 等；
- “身份/角色/资格”的技术治理页签；
- “租户工作空间”“平台级实体”“tenant_id”等事实所有权说明。

这违反冻结的人类词汇合同：Level A 首层不出现内部 ID、DDD 类型名或事实所有权说明；技术信息应进入独立治理台。当前项目的 Admin 已把业务模式默认区切到 `content`，标题为“业务运营工作台”，见 `frontend/apps/admin/src/App.vue:227-240`。

同时，本原型把 Organization 管理全部放入技术模式；当前项目在业务模式仍有“组织与成员”入口，见 `App.vue:77-92,568-570`。事实归属平台级，不等于所有人类组织成员操作都属于技术治理。

整改要求：

1. 业务模式默认进入内容/赛事运营首页，不进入身份授权模型页。
2. 业务模式使用“人员、工作职责、单位成员、赛事审批”等 Level A/B 语言。
3. 技术模式再展示 Subject、Identity、RoleAssignment、Scope、raw ID 和兼容迁移状态。
4. 保留业务模式中的组织成员运营入口；创建 canonical Organization、稳定代码和兼容治理可放技术模式。

### P1-03：参赛学生授权示例混入 Organization scope

`操作概览 (10).md:367` 把“参赛学生”角色写成：

`subject_id + role_id + scope_tenant_id + scope_organization_id`

但同一文件将参赛学生定义为 TENANT 级角色，`项目概览 (7).md:795` 也标明 `TENANT_ADMIN`/租户角色使用 TENANT Context。TENANT 与 ORGANIZATION 是独立 Scope，不应为了表示人员所属单位就在 TENANT assignment 中附加 Organization scope；Membership 已单独表达现实组织关系。

影响：会把“现实组织归属”重新编码进权限 Scope，破坏精确 Context 匹配和跨 Tenant Organization 默认拒绝规则。

整改要求：参赛学生 TENANT assignment 只使用 Tenant scope；只有组织审批/组织管理员等 ORGANIZATION Context 才同时解析业务 Tenant 与平台 Organization。

## 6. 非阻断但必须整改的问题

### P2-01：Tenant 人类词汇仍容易退化成“赛事租户”

本轮已经删除“Tenant = 赛事”的直接等式，这是正确改进；但仍大量使用“竞赛赛事租户”“赛事租户”“数学竞赛运营租户”，例如：

- `操作概览 (10).md:281,303,574,620`
- `项目概览 (7).md:74,763-764,796`
- `admin-demo (10).html:175,197,216`

冻结词汇合同规定 Tenant 的 Level A 是“当前单位”，Level B 是“租户单位”，Activity 才是“比赛/赛事”。

整改建议：

- 业务页面显示“当前单位：全国大学生数学竞赛组委会”与“当前赛事：第十二届数学竞赛”；
- 技术详情可显示“租户单位 / Tenant ID”；
- 文档示例至少展示一个 Tenant 下有两届或两个 Activity，避免靠名称重新暗示二者等价。

### P2-02：Web Level A 仍暴露内部 Credential ID

`web-demo (10).html:837-838` 在“我的证书”直接显示 `凭证 ID：CRED-*`。冻结词汇合同要求 Credential 按用途显示为证书、获奖证明或通行凭证，内部 ID 不应默认进入 Level A。

整改建议：首层显示证书名称、签发机构、日期和状态；技术编号放入“查看详情/验证信息”。

### P3-01：语义化和键盘可操作性仍未达到可验收状态

浏览器 DOM 统计：

| 原型 | `[onclick]` 总数 | 非 `button/a/input/select/textarea` 点击目标 | 无 `href` 的 `<a>` |
|---|---:|---:|---:|
| Web | 144 | 39 | 26 |
| Admin | 359 | 86 | 0 |

大量导航在可访问性树中被识别为普通 `generic`，无法稳定获得链接/按钮语义；Admin 也有较多以 `<div onclick>` 实现的菜单和页签。

整改建议：使用真实 `<button type="button">`、带 `href` 的 `<a>`、语义化 `<nav>`/tablist；补充键盘焦点、`aria-current`、modal focus trap、Esc 关闭和回焦。

## 7. 浏览器复核结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| Web 初始头像 | PASS | 登录按钮可见，用户头像隐藏 |
| Web 私有导航隔离 | FAIL | 未登录可直接进入工作台 |
| Web 能力来源 | FAIL | 用户可自行切换 student/reviewer/orgadmin |
| Web 身份信息顺序 | PASS | 已按冻结顺序调整 |
| Admin 模式按钮 | PASS | 业务/技术模式按钮存在且可切换 |
| Admin 业务默认页 | FAIL | 仍默认进入身份、角色与资格治理页 |
| Admin 授权表单 | FAIL | 所有上下文统一提交 Subject，与当前接口不兼容 |
| 运行时 JS 错误 | PASS | 两个原型均未发现 console error/warn |
| 基础可访问性 | FAIL | 大量无语义点击目标与无 href 锚点 |

## 8. 第四轮最低通过条件

下一轮必须同时满足：

1. 删除 Organization 创建流程中的自动 Membership 和自动管理授权。
2. 把“目标 Subject 模型”与“当前兼容 API/历史 legacy”明确分层；不再宣称全部路径只使用 `subject_role_assignment`。
3. Admin 授权表单与当前 API 一致：ORGANIZATION 用 `subjectId`，其他 Context 用 `identityId`；或者明确标记未来统一 API 且不得伪装成当前可调用接口。
4. Web 未登录不可进入工作台、消息、我的等私有页面。
5. 删除客户端自选角色/能力；工作台内容和可用上下文只来自服务端投影。
6. Admin 业务模式默认进入人类业务运营页，技术治理对象进入技术模式。
7. 修正参赛学生 scope 示例，不把 Organization scope 混入 TENANT assignment。
8. 提交“本报告条目 → 修改文件/行 → 浏览器验证结果”的整改对照表。

建议同时完成：统一 Tenant/Activity 人类词汇、隐藏 Credential 内部 ID、修复主要交互控件的语义化和键盘路径。

## 9. 最终判定

第三轮相较第二轮有明显且可验证的进展，Root Model 的大方向已经接近当前项目；但当前运行契约、认证/授权边界和 Admin 人类模式仍存在会误导实现的偏差。

因此，本轮结论为：

> **REJECTED_WITH_MATERIAL_PROGRESS — 不批准冻结，整改 P0/P1 后进入第四轮复审。**

审查过程中未修改四份受审文件，也未修改当前项目源代码；仅生成本报告。
