<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teaching.content.mapper.QuestionsMapper">

    <resultMap type="Questions" id="QuestionsResult">
        <result property="id"    column="id"    />
        <result property="questions"    column="questions"    />
        <result property="answer"    column="answer"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="version"    column="version"    />
        <result property="delFlag"    column="del_flag"    />
        <result property="remark"    column="remark"    />
        <result property="userId"    column="user_id"    />
        <result property="orgId"    column="org_id"    />
    </resultMap>

    <sql id="selectQuestionsVo">
        select id, questions, answer, create_by, create_time, update_by, update_time, version, del_flag, remark, user_id, org_id from questions
    </sql>

    <select id="selectQuestionsList" parameterType="Questions" resultMap="QuestionsResult">
        <include refid="selectQuestionsVo"/>
        where del_flag = '0'
            <if test="questions != null  and questions != ''"> and questions like concat('%' ,#{questions},'%')</if>
            <if test="answer != null  and answer != ''"> and answer like concat('%', #{answer} , '%')</if>
            <if test="version != null "> and version = #{version}</if>
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="orgId != null "> and org_id = #{orgId}</if>
            <if test="keyWord != null and keyWord !=''"> and (questions like concat('%' ,#{keyWord},'%') or answer like concat('%' ,#{keyWord},'%'))</if>
    </select>

    <select id="selectQuestionsById" parameterType="Long" resultMap="QuestionsResult">
        <include refid="selectQuestionsVo"/>
        where id = #{id}
    </select>

    <insert id="insertQuestions" parameterType="Questions" useGeneratedKeys="true" keyProperty="id">
        insert into questions
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="questions != null and questions != ''">questions,</if>
            <if test="answer != null and answer != ''">answer,</if>
            <if test="createBy != null">create_by,</if>
            create_time,
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="version != null">version,</if>
            <if test="delFlag != null">del_flag,</if>
            <if test="remark != null">remark,</if>
            <if test="userId != null">user_id,</if>
            <if test="orgId != null">org_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="questions != null and questions != ''">#{questions},</if>
            <if test="answer != null and answer != ''">#{answer},</if>
            <if test="createBy != null">#{createBy},</if>
            sysdate(),
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="version != null">#{version},</if>
            <if test="delFlag != null">#{delFlag},</if>
            <if test="remark != null">#{remark},</if>
            <if test="userId != null">#{userId},</if>
            <if test="orgId != null">#{orgId},</if>
        </trim>
    </insert>

    <update id="updateQuestions" parameterType="Questions">
        update questions
        <trim prefix="SET" suffixOverrides=",">
            <if test="questions != null and questions != ''">questions = #{questions},</if>
            <if test="answer != null and answer != ''">answer = #{answer},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            update_time = sysdate(),
            version = version + 1,
            <if test="delFlag != null">del_flag = #{delFlag},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="orgId != null">org_id = #{orgId},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteQuestionsById" parameterType="Long">
        delete from questions where id = #{id}
    </delete>

    <delete id="deleteQuestionsByIds" parameterType="String">
        delete from questions where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
