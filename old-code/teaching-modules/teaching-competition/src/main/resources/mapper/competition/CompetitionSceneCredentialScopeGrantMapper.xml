<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teaching.competition.mapper.CompetitionSceneCredentialScopeGrantMapper">

    <resultMap type="com.teaching.competition.domain.CompetitionSceneCredentialScopeGrant"
               id="CompetitionSceneCredentialScopeGrantResult">
        <id property="grantId" column="grant_id"/>
        <result property="credentialId" column="credential_id"/>
        <result property="competitionSeriesId" column="competition_series_id"/>
        <result property="scopeType" column="scope_type"/>
        <result property="scopeRefId" column="scope_ref_id"/>
        <result property="activeGrantKey" column="active_grant_key"/>
        <result property="sourceType" column="source_type"/>
        <result property="sourceScheduleId" column="source_schedule_id"/>
        <result property="sourceTargetId" column="source_target_id"/>
        <result property="credentialType" column="credential_type"/>
        <result property="roleCode" column="role_code"/>
        <result property="subjectType" column="subject_type"/>
        <result property="subjectCode" column="subject_code"/>
        <result property="abilityJson" column="ability_json"/>
        <result property="validFrom" column="valid_from"/>
        <result property="validTo" column="valid_to"/>
        <result property="operationWindowJson" column="operation_window_json"/>
        <result property="grantStatus" column="grant_status"/>
        <result property="grantSnapshotJson" column="grant_snapshot_json"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <sql id="selectGrantColumns">
        select grant_id, credential_id, competition_series_id, scope_type, scope_ref_id, active_grant_key,
               source_type, source_schedule_id, source_target_id, credential_type, role_code,
               subject_type, subject_code, ability_json, valid_from, valid_to,
               operation_window_json, grant_status, grant_snapshot_json,
               create_by, create_time, update_by, update_time, deleted
        from competition_scene_credential_scope_grant
    </sql>

    <select id="selectGrantById" parameterType="Long" resultMap="CompetitionSceneCredentialScopeGrantResult">
        <include refid="selectGrantColumns"/>
        where grant_id = #{grantId}
          and deleted = 0
    </select>

    <select id="selectActiveGrantsByCredential" resultMap="CompetitionSceneCredentialScopeGrantResult">
        <include refid="selectGrantColumns"/>
        where credential_id = #{credentialId}
          and grant_status = 'ACTIVE'
          and deleted = 0
        order by scope_type, scope_ref_id, grant_id
    </select>

    <select id="selectActiveScheduleGrants" resultMap="CompetitionSceneCredentialScopeGrantResult">
        <include refid="selectGrantColumns"/>
        where credential_id = #{credentialId}
          and scope_type = 'SCHEDULE'
          and scope_ref_id = #{scheduleId}
          and grant_status = 'ACTIVE'
          and deleted = 0
        order by grant_id desc
    </select>

    <select id="selectActiveScheduleGrant" resultMap="CompetitionSceneCredentialScopeGrantResult">
        <include refid="selectGrantColumns"/>
        where credential_id = #{credentialId}
          and scope_type = 'SCHEDULE'
          and scope_ref_id = #{scheduleId}
          and grant_status = 'ACTIVE'
          and deleted = 0
        <choose>
            <when test="sourceTargetId != null">
                and source_target_id = #{sourceTargetId}
            </when>
            <otherwise>
                and source_target_id is null
            </otherwise>
        </choose>
        order by grant_id desc
        limit 1
    </select>

    <select id="selectActiveScheduleGrantForUpdate" resultMap="CompetitionSceneCredentialScopeGrantResult">
        <include refid="selectGrantColumns"/>
        where credential_id = #{credentialId}
          and scope_type = 'SCHEDULE'
          and scope_ref_id = #{scheduleId}
          and grant_status = 'ACTIVE'
          and deleted = 0
        <choose>
            <when test="sourceTargetId != null">
                and source_target_id = #{sourceTargetId}
            </when>
            <otherwise>
                and source_target_id is null
            </otherwise>
        </choose>
        order by grant_id desc
        limit 1
        for update
    </select>

    <insert id="insertGrant"
            parameterType="com.teaching.competition.domain.CompetitionSceneCredentialScopeGrant"
            useGeneratedKeys="true" keyProperty="grantId" keyColumn="grant_id">
        insert into competition_scene_credential_scope_grant
        (credential_id, competition_series_id, scope_type, scope_ref_id, active_grant_key, source_type,
         source_schedule_id, source_target_id, credential_type, role_code, subject_type,
         subject_code, ability_json, valid_from, valid_to, operation_window_json,
         grant_status, grant_snapshot_json, create_by, create_time, update_by, update_time, deleted)
        values
        (#{credentialId}, #{competitionSeriesId}, #{scopeType}, #{scopeRefId}, #{activeGrantKey}, #{sourceType},
         #{sourceScheduleId}, #{sourceTargetId}, #{credentialType}, #{roleCode}, #{subjectType},
         #{subjectCode}, #{abilityJson}, #{validFrom}, #{validTo}, #{operationWindowJson},
         #{grantStatus}, #{grantSnapshotJson}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime}, #{deleted})
    </insert>

    <update id="updateGrantMutableFields"
            parameterType="com.teaching.competition.domain.CompetitionSceneCredentialScopeGrant">
        update competition_scene_credential_scope_grant
        <set>
            <if test="sourceType != null">source_type = #{sourceType},</if>
            <if test="sourceScheduleId != null">source_schedule_id = #{sourceScheduleId},</if>
            <if test="activeGrantKey != null">active_grant_key = #{activeGrantKey},</if>
            <if test="credentialType != null and credentialType != ''">credential_type = #{credentialType},</if>
            <if test="roleCode != null">role_code = #{roleCode},</if>
            <if test="subjectType != null and subjectType != ''">subject_type = #{subjectType},</if>
            <if test="subjectCode != null and subjectCode != ''">subject_code = #{subjectCode},</if>
            <if test="abilityJson != null">ability_json = #{abilityJson},</if>
            <if test="validFrom != null">valid_from = #{validFrom},</if>
            <if test="validTo != null">valid_to = #{validTo},</if>
            <if test="operationWindowJson != null">operation_window_json = #{operationWindowJson},</if>
            <if test="grantSnapshotJson != null">grant_snapshot_json = #{grantSnapshotJson},</if>
            update_by = #{updateBy},
            update_time = #{updateTime}
        </set>
        where grant_id = #{grantId}
          and grant_status = 'ACTIVE'
          and deleted = 0
    </update>

    <update id="revokeGrant">
        update competition_scene_credential_scope_grant
        set grant_status = 'REVOKED',
            active_grant_key = null,
            update_by = #{updateBy},
            update_time = now()
        where grant_id = #{grantId}
          and grant_status = 'ACTIVE'
          and deleted = 0
    </update>

    <update id="revokeGrantsByTarget">
        update competition_scene_credential_scope_grant
        set grant_status = 'REVOKED',
            active_grant_key = null,
            update_by = #{updateBy},
            update_time = now()
        where source_type = 'SCHEDULE_TARGET'
          and source_schedule_id = #{sourceScheduleId}
          and source_target_id = #{sourceTargetId}
          and grant_status = 'ACTIVE'
          and deleted = 0
    </update>
</mapper>
