<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teaching.system.mapper.SysRoleMenuMapper">

	<resultMap type="com.teaching.system.api.domain.SysRoleMenu" id="SysRoleMenuResult">
		<result property="roleId"    column="role_id"    />
		<result property="menuId"    column="menu_id"    />
		<result property="dataScope"    column="data_scope"    />
		<result property="createBy"    column="create_by"    />
		<result property="createTime"    column="create_time"    />
		<result property="updateBy"    column="update_by"    />
		<result property="updateTime"    column="update_time"    />
		<result property="version"    column="version"    />
		<result property="delFlag"    column="del_flag"    />
	</resultMap>

	<sql id="selectSysRoleMenuVo">
		select role_id, menu_id, data_scope, create_by, create_time, update_by, update_time, version, del_flag from sys_role_menu
	</sql>

	<select id="checkMenuExistRole" resultType="Integer">
	    select count(1) from sys_role_menu where menu_id = #{menuId}
	</select>

	<delete id="deleteRoleMenuByRoleId" parameterType="Long">
		delete from sys_role_menu where role_id=#{roleId}
	</delete>

	<delete id="deleteRoleMenu" parameterType="Long">
 		delete from sys_role_menu where role_id in
 		<foreach collection="array" item="roleId" open="(" separator="," close=")">
 			#{roleId}
        </foreach>
 	</delete>

	<select id="selectSysRoleMenuByRoleId" parameterType="Long" resultMap="SysRoleMenuResult">
		<include refid="selectSysRoleMenuVo"/>
		where role_id = #{roleId}
	</select>

	<insert id="insertSysRoleMenu">
		insert into sys_role_menu(role_id, menu_id,data_scope,create_by,create_time)
		values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.roleId},#{item.menuId},#{item.dataScope},#{item.createBy},sysdate())
		</foreach>
	</insert>

	<update id="updateSysRoleMenu" parameterType="com.teaching.system.api.domain.SysRoleMenu">
		update sys_role_menu
		<trim prefix="SET" suffixOverrides=",">
			<if test="menuId != null">menu_id = #{menuId},</if>
			<if test="dataScope != null">data_scope = #{dataScope},</if>
			<if test="createBy != null">create_by = #{createBy},</if>
			<if test="createTime != null">create_time = #{createTime},</if>
			<if test="updateBy != null">update_by = #{updateBy},</if>
			<if test="updateTime != null">update_time = #{updateTime},</if>
			<if test="version != null">version = #{version},</if>
			<if test="delFlag != null">del_flag = #{delFlag},</if>
		</trim>
		where role_id = #{roleId}
	</update>
</mapper>
