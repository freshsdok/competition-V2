<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teaching.system.mapper.SysRoleMapper">

    <resultMap type="com.teaching.system.api.domain.SysRole" id="SysRoleResult">
        <id property="roleId" column="role_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="menuCheckStrictly" column="menu_check_strictly"/>
        <result property="deptCheckStrictly" column="dept_check_strictly"/>
        <result property="orgCheckStrictly" column="org_check_strictly"/>
        <result property="acquMethod" column="acqu_method"/>
        <result property="lockFlag" column="lock_flag"/>
        <result property="exclusionFlag" column="exclusion_flag"/>
        <result property="roleType" column="role_type"/>
        <result property="exclusionLevel" column="exclusion_level"/>
		<result property="status" column="status"/>
        <result property="studentFlag" column="student_flag"/>
        <result property="teacherFlag" column="teacher_flag"/>
        <result property="authFlag" column="auth_flag"/>
        <result property="competitionFlag" column="competition_flag"/>
        <result property="captainFlag" column="captain_flag"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectRoleVo">
        select distinct r.role_id, r.role_name, r.role_key, r.role_sort, r.menu_check_strictly,
        r.dept_check_strictly,
        r.org_check_strictly,r.acqu_method,r.lock_flag,r.exclusion_flag,r.role_type,r.exclusion_level,
        r.status, r.student_flag, r.teacher_flag, r.auth_flag, r.competition_flag,r.captain_flag,r.del_flag, r.create_time, r.remark
        from sys_role r
        left join sys_user_role ur on ur.role_id = r.role_id
        left join sys_user u on u.user_id = ur.user_id
        left join sys_org d on u.org_id = d.org_id
    </sql>

    <select id="selectRoleList" parameterType="com.teaching.system.api.domain.SysRole" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.del_flag = '0'
        <if test="roleId != null and roleId != 0">
            AND r.role_id = #{roleId}
        </if>
        <if test="roleName != null and roleName != ''">
            AND r.role_name like concat('%', #{roleName}, '%')
        </if>
        <if test="status != null and status != ''">
            AND r.status = #{status}
        </if>
        <if test="roleKey != null and roleKey != ''">
            AND r.role_key like concat('%', #{roleKey}, '%')
        </if>
        <if test="params.beginTime != null and params.beginTime != ''"><!-- 开始时间检索 -->
            and date_format(r.create_time,'%Y%m%d') &gt;= date_format(#{params.beginTime},'%Y%m%d')
        </if>
        <if test="params.endTime != null and params.endTime != ''"><!-- 结束时间检索 -->
            and date_format(r.create_time,'%Y%m%d') &lt;= date_format(#{params.endTime},'%Y%m%d')
        </if>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by r.exclusion_flag desc,r.role_sort
    </select>

    <select id="selectRolePermissionByUserId" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0' and ur.user_id = #{userId}
    </select>

    <select id="selectRoleAll" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
    </select>

    <select id="selectRoleListByUserId" parameterType="Long" resultType="Long">
        select r.role_id
        from sys_role r
        left join sys_user_role ur on ur.role_id = r.role_id
        left join sys_user u on u.user_id = ur.user_id
        where u.user_id = #{userId}
    </select>

    <select id="selectRoleByFlag" parameterType="com.teaching.system.api.domain.SysRole" resultMap="SysRoleResult">
        select distinct r.role_id, r.role_name, r.role_key, r.role_sort, r.menu_check_strictly,
        r.dept_check_strictly,
        r.org_check_strictly,r.acqu_method,r.lock_flag,r.exclusion_flag,r.role_type,r.exclusion_level,
        r.status, r.student_flag, r.teacher_flag, r.auth_flag, r.competition_flag,r.captain_flag,r.del_flag, r.create_time, r.remark
        from sys_role r
        where r.del_flag = '0'
        <if test="studentFlag">
            and r.student_flag = #{studentFlag}
        </if>
        <if test="teacherFlag">
            and r.teacher_flag = #{teacherFlag}
        </if>
        <if test="authFlag">
            and r.auth_flag = #{authFlag}
        </if>
        <if test="competitionFlag">
            and r.competition_flag = #{competitionFlag}
        </if>
        <if test="captainFlag">
            and r.captain_flag = #{captainFlag}
        </if>
    </select>

    <select id="selectRoleById" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_id = #{roleId}
    </select>

    <select id="selectRolesByUserName" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0' and u.user_name = #{userName}
    </select>

    <select id="checkRoleNameUnique" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_name=#{roleName} and r.del_flag = '0' limit 1
    </select>

    <select id="checkRoleKeyUnique" parameterType="String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_key=#{roleKey} and r.del_flag = '0' limit 1
    </select>

    <select id="checkIndependentRoleUnique" parameterType="com.teaching.system.api.domain.SysRole" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_id = #{roleId} and r.exclusion_level = #{exclusionLevel} and r.del_flag = '0' limit 1
    </select>

    <insert id="insertRole" parameterType="com.teaching.system.api.domain.SysRole" useGeneratedKeys="true"
            keyProperty="roleId">
        insert into sys_role(
        <if test="roleId != null and roleId != 0">role_id,</if>
        <if test="roleName != null and roleName != ''">role_name,</if>
        <if test="roleKey != null and roleKey != ''">role_key,</if>
        <if test="roleSort != null">role_sort,</if>
        <if test="menuCheckStrictly != null">menu_check_strictly,</if>
        <if test="deptCheckStrictly != null">dept_check_strictly,</if>
        <if test="orgCheckStrictly != null">org_check_strictly,</if>
        <if test="acquMethod != null">acqu_method,</if>
        <if test="lockFlag != null">lock_flag,</if>
        <if test="exclusionFlag != null">exclusion_flag,</if>
        <if test="roleType != null">role_type,</if>
        <if test="status != null and status != ''">status,</if>
        <if test="studentFlag != null">student_flag,</if>
        <if test="teacherFlag != null">teacher_flag,</if>
        <if test="authFlag != null">auth_flag,</if>
        <if test="competitionFlag != null">competition_flag,</if>
        <if test="captainFlag != null">captain_flag,</if>
        <if test="remark != null and remark != ''">remark,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        create_time
        )values(
        <if test="roleId != null and roleId != 0">#{roleId},</if>
        <if test="roleName != null and roleName != ''">#{roleName},</if>
        <if test="roleKey != null and roleKey != ''">#{roleKey},</if>
        <if test="roleSort != null">#{roleSort},</if>
        <if test="menuCheckStrictly != null">#{menuCheckStrictly},</if>
        <if test="deptCheckStrictly != null">#{deptCheckStrictly},</if>
        <if test="orgCheckStrictly != null">#{orgCheckStrictly},</if>
        <if test="acquMethod != null and acquMethod != ''">#{acquMethod},</if>
        <if test="lockFlag != null">#{lockFlag},</if>
        <if test="exclusionFlag != null">#{exclusionFlag},</if>
        <if test="roleType != null and roleType != ''">#{roleType},</if>
        <if test="status != null and status != ''">#{status},</if>
        <if test="studentFlag != null">#{studentFlag},</if>
        <if test="teacherFlag != null">#{teacherFlag},</if>
        <if test="authFlag != null">#{authFlag},</if>
        <if test="competitionFlag != null">#{competitionFlag},</if>
        <if test="captainFlag != null">#{captainFlag},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        sysdate()
        )
    </insert>

    <update id="updateRole" parameterType="com.teaching.system.api.domain.SysRole">
        update sys_role
        <set>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="roleKey != null and roleKey != ''">role_key = #{roleKey},</if>
            <if test="roleSort != null">role_sort = #{roleSort},</if>
            <if test="menuCheckStrictly != null">menu_check_strictly = #{menuCheckStrictly},</if>
            <if test="deptCheckStrictly != null">dept_check_strictly = #{deptCheckStrictly},</if>
			<if test="orgCheckStrictly != null">org_check_strictly= #{orgCheckStrictly},</if>
			<if test="acquMethod != null and acquMethod != ''">acqu_method= #{acquMethod},</if>
			<if test="lockFlag != null">lock_flag= #{lockFlag},</if>
			<if test="exclusionFlag != null">exclusion_flag= #{exclusionFlag},</if>
			<if test="roleType != null and roleType != ''">role_type= #{roleType},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="studentFlag != null">student_flag= #{studentFlag},</if>
            <if test="teacherFlag != null">teacher_flag= #{teacherFlag},</if>
            <if test="authFlag != null">auth_flag= #{authFlag},</if>
            <if test="competitionFlag != null">competition_flag= #{competitionFlag},</if>
            <if test="captainFlag != null">captain_flag= #{captainFlag},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            update_time = sysdate()
        </set>
        where role_id = #{roleId}
    </update>

    <delete id="deleteRoleById" parameterType="Long">
        update sys_role set del_flag = '2' where role_id = #{roleId}
    </delete>

    <delete id="deleteRoleByIds" parameterType="Long">
        update sys_role set del_flag = '2' where role_id in
        <foreach collection="array" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>

    <select id="selectRoleByIds"  resultMap="SysRoleResult">
            select role_id,exclusion_flag from sys_role where role_id in
        <foreach collection="array" item="roleId" open="(" separator="," close=")">
                #{roleId}
        </foreach>
    </select>

</mapper>
