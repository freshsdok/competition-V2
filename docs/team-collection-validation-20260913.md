# V1 队伍办理验证记录

日期：2026-09-13。代码基线：`daca41a56deba06cfa536fd03450d39dd74ea45f`，结果对应本次未提交工作区变更。

## 已执行

| 检查 | 结果 | 证据范围 |
|---|---|---|
| V1 后端 reactor 编译及指定测试 | 16 passed / 0 failed / 0 errors / 0 skipped | 9项真实MySQL测试、2项控制器身份/入口测试、5项既有桥接回归 |
| PC Vue组件与入口路径测试 | 10 passed | Happy DOM组件交互；不等于真实浏览器E2E |
| 名单生成工具测试 | 4 passed | 教师排除、重复队伍/公式拒绝、SQL注入防护、确定性输出 |
| PC production build | PASS | 输出到 `/tmp/deshi-team-collection-pc-build`，没有覆盖仓库内既有构建目录 |
| `git diff --check` | PASS | 已跟踪变更无空白错误 |

后端命令及环境见实现说明。首次测试因没有默认Docker socket失败，使用实际Colima socket后又遇到客户端API 1.32低于服务端最低1.40。最终以进程参数 `-Dapi.version=1.44` 通过，未修改Docker守护进程或业务运行配置。另修正了一处Mockito重设异常桩的测试写法后，整组重跑通过。

## MySQL 测试覆盖

- 双成员同时竞争一个队伍，只有一个事务取得办理权，另一人不能取得入口。
- 放弃后重新申请，旧页面即使是同一用户也不能用旧版本确认、释放或进入。
- 确认重试、释放重试不会重复写入事件。
- 已确认完成时保留办理人姓名与UTC确认时间，其他成员视图没有银行卡/证件/联系方式字段。
- 非名单、其他队伍成员、被撤销成员、停用账号不能取得资格。
- 桥接失败保留当前占用且不泄露上游错误详情。
- 事件插入失败时，队伍状态与持有人更新一起回滚。
- 功能关闭时不查询新增表，旧收款桥接也不能无条件签发。
- 在隔离MySQL中导入完整520队/1216学生源记录，使用**合成账号及成员关系**执行预览、教师角色排除、审核参数保护、发布、重复发布保持已确认状态、管理员恢复版本保护。

名单导入测试仅证明SQL和限制条件能工作，不能证明合成账号就是实际学生账号。

## 前端测试覆盖

首次打开不签发V2入口；申请成功后才打开iframe；同队其他学生只读姓名与时间；确认完成需显式对话框；放弃后关闭窗口；取消确认或离开页面不释放；状态查询失败关闭窗口并禁用操作；无资格无入口；迟到的入口响应不能重开已释放轮次；抢占失败不调用V2。

构建保留既有 Browserslist 数据陈旧及大chunk提示，本轮不扩大到依赖升级或构建优化。

## 尚未执行

- `NOT_EXECUTED`：实际 V1 名单映射审核、发布；只读匹配核查见下文。
- `NOT_EXECUTED`：本助手执行业务数据库建表或数据写入（用户已自行运行建表及01源名单导入）。
- `NOT_EXECUTED`：实际双账号浏览器办理、短信验证与原V2页面保存完整E2E。
- `NOT_EXECUTED`：部署、启用开关、Git提交或推送。

新功能默认关闭。V2工作区原有9个已跟踪文件的改动统计仍为108行新增/17行删除，本轮没有写入V2。

## 本地名单匹配故障排查

2026-09-13，用户授权读取本地3306。JDBC实际连接数据库 `jiaoxue_test`，MySQL `8.0.46`，服务器主机标识 `99bcc0a3bfc8`。连接设置只读并使用只读事务，全部诊断为SELECT，结束ROLLBACK；未执行02中的临时建表语句、03发布或任何业务写入。

- 初版条件与现场状态不符：1216条报名check_status全为NULL；1213条已有账号的同队成员check_status也为NULL；520队的队伍check_status均为4。1213条关联账号均未删除、未停用。
- 02/05修正为本次固定来源及81届次兼容历史NULL，同时要求队伍审核通过；角色、删除/停用状态、账号关联与唯一性检查保留。
- 执行修正后02候选SELECT的只读等价聚合：1213个名单位置、520队、1206个不同账号，候选账号或队伍有歧义的已匹配位置为0，同队同账号重复槽位为0。人数不能直接按1213报出，同一账号可能出现在不同队伍。
- 3条缺少账号：赛道一名单E265、F354，赛道二名单H21；报名和同名成员关系均没有user_id。分别存在唯一的姓名、证件号及类型一致、实名认证通过的正常账号。06只读列出候选和辅助核查标志，不把候选当已核准映射。
- 读取时新功能队伍表及成员表均为0行；先前03错误未发布名单。
- 本次针对映射变更重跑 `TeamCollectionMySqlTest#sourceImportPreviewPublishAndRecoveryAreGuardedAndRepeatable`：1项通过，0失败/错误/跳过。测试新增历史双NULL兼容、3个缺失账号阻止发布、明确报名/成员拒绝状态、未审核队伍和其他届次NULL拒绝；其余业务代码未变，本次未重复运行前端或全量测试。

这些证据仅反映本地库此次快照，不代表生产库，也不证明V2保存或浏览器完整办理流程。

## 用户确认后的三条名单补充

用户明确确认三名参赛者与候选账号一致后，新增07专项脚本，只补充本次会话预览，由03持久发布名单；不更新原报名/成员/实名记录。本次未在业务库执行07或03。

重跑同一MySQL导入集成测试：1项通过、0失败/错误/跳过。合成实名数据验证了：缺失审核参数/届次不符拒绝；实名不通过、账号停用、成员角色改为教师、实名出现第二个账号时，三条均不补充；成功执行及重复执行后1216个位置完整匹配；原报名和成员表的3个user_id仍为NULL；03实际写入隔离容器名单且包含三个确认账号；重复发布与办理状态保护仍通过。测试证件数据为SYNTHETIC占位值，没有复制真实证件或联系方式。

## 保留限制

人工确认不等于V2保存成功；V1释放或完成不吊销V2已打开的会话；不做银行卡真实性核验。以上为本轮用户确认的简化方案边界。

## 迁移文件整理后的验证

当前操作入口为 `db/team_collection/README.md`。目录保留01源数据和02统一导入两个SQL，旧预览/发布/诊断/补充文件已删除；上文出现的旧编号属于历史排查记录，不再用于执行。管理员恢复迁移到 `db/maintenance/team_collection_recovery.sql`。

整理后重跑导入MySQL集成测试，1项通过、0失败/错误/跳过。该测试使用完整源名单及合成账号，验证：首次源导入及重跑、参数保护、预览不发布、历史NULL兼容、三条确认账号补齐、角色/状态/实名歧义拒绝且不产生部分名单、正式导入重建临时结果、520队/1216成员写入、原报名/成员的NULL绑定保持不变、重复导入保留已确认办理状态、撤销成员不被重新启用、维护恢复版本校验。未执行本地业务数据库迁移；前后端产品代码未因本次整理而改变。
