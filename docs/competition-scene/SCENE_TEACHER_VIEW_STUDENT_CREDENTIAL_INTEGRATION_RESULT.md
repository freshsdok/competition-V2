# 教师查看自己指导学生参赛证测试库联调结果

生成时间：2026-07-07 23:55

## 1. 运行态验证

运行态验证未通过。

已执行：

- UAT 健康检查脚本；
- 本机和候选测试地址端口探测；
- PC 前端代理配置检查；
- 登录接口代码路径检查。

结果：

- gateway / auth / system / competition / PC 前端均未运行；
- 测试库候选地址不可达；
- Nacos 候选地址不可达；
- 仅本机 Redis 可用；
- 无法确认正式测试用 competition 服务已加载本次代码；
- 无法确认 PC 前端已部署或加载新构建。

详见：

- `docs/competition-scene/SCENE_TEACHER_VIEW_STUDENT_CREDENTIAL_RUNTIME_VERIFY.md`

## 2. 测试数据

未能选取或准备真实测试数据。

原因：

- 测试库不可达；
- 未提供可用测试账号；
- 未连接生产数据库；
- 未写入测试库数据。

详见：

- `docs/competition-scene/SCENE_TEACHER_VIEW_STUDENT_CREDENTIAL_TEST_DATA.md`

## 3. 教师 A 正向验证

未执行。

阻塞原因：

- 无教师 A 登录 token；
- 无法调用 `GET /competition/userCompetition/teacher/studentCredentials`；
- 无法选取团队 1、学生 1、学生 2；
- 无法验证 `NOT_GENERATED`、现场状态和赛场安排。

当前结论：未验证。

## 4. 教师 B 隔离验证

未执行。

阻塞原因：

- 无教师 B 登录 token；
- 无法选取团队 2；
- 无法验证教师 B 只能看到团队 2 且不能看到团队 1。

当前结论：未验证。

## 5. 普通学生权限验证

未执行。

阻塞原因：

- 无普通学生登录 token；
- 无法调用教师接口；
- 无法验证普通学生接口响应为空或无权限。

当前结论：未验证。

## 6. teamCode 越权验证

未执行。

计划用例：

- 教师 A 传入教师 B 团队 `teamCode`；
- 教师 A 传入非自己指导团队的 `competitionSeriesId + teamCode`；
- 期望不返回非授权团队和证件。

阻塞原因：

- 无教师 A token；
- 无教师 B 团队数据；
- 运行态不可达。

当前结论：未验证。

## 7. credentialId 越权验证

未执行。

计划用例：

- 教师 A 查询教师 B 团队学生的 `credentialId`；
- 学生账号查询他人 `credentialId`；
- 期望返回“无权限查看该学生参赛证”或统一无权限响应。

阻塞原因：

- 无登录 token；
- 无跨团队 `credentialId`；
- 运行态不可达。

当前结论：未验证。

## 8. 详情接口验证

未执行。

计划用例：

- 教师 A 查询自己学生 `credentialId` 成功；
- 响应包含证件编号、证件状态、赛场信息；
- 教师 A 查询非自己学生 `credentialId` 被拒绝；
- 学生账号查询他人 `credentialId` 被拒绝；
- 不存在或已删除 `credentialId` 返回友好错误。

阻塞原因：

- 运行态不可达；
- 无测试数据；
- 无登录 token。

当前结论：未验证。

## 9. 安全字段验证

未执行真实响应验证。

本轮未获取接口响应，因此无法在真实数据中确认响应是否不含以下字段：

- 身份证号；
- `openId`;
- `unionId`;
- `credentialToken`;
- 未脱敏手机号；
- 日志中的 `qrContent` 或 token。

基于开发结果的静态口径：

- 新增教师 VO 未定义 `credentialToken`；
- 新增教师 SQL 未选择身份证号、`openId`、`unionId`、手机号；
- 新增教师 SQL 选择了 `qr_content` 用于授权教师范围内二维码展示；
- 真实响应仍需在测试环境中复核。

当前结论：未完成真实响应验证。

## 10. PC 页面验证

未执行浏览器验证。

阻塞原因：

- PC 前端服务未运行；
- 无正式测试 PC 地址；
- 无教师 A / 非教师账号；
- 后端接口不可达。

未验证项目：

- “指导学生参赛证”区域是否显示；
- 团队是否按赛事/团队分组；
- 已生成证件学生是否显示“查看参赛证”；
- 未生成证件学生是否显示“未生成参赛证”；
- 点击后是否弹出二维码/证件详情；
- 是否展示报道、资料、候场状态；
- 是否没有修改/确认类按钮；
- 非教师或无指导团队用户是否为空状态。

当前结论：未验证。

## 11. 真实数据口径问题

由于测试库不可达，本轮无法基于真实数据确认以下问题：

1. `competition_role_name = 指导教师 / 指导老师` 两种口径是否同时存在；
2. `leader_teacher_id` 在真实报名数据中是否稳定有值；
3. `pay_status = paid` 是否会漏掉线下导入学生；
4. `check_status = 4` 是否会漏掉已生成参赛证学生；
5. 团队级证件在真实数据中是否会造成同一团队成员列表重复展示同一团队证件；
6. 已删除证件、历史证件、不同 `subject_type` 的真实分布。

注意：本轮未直接扩大过滤范围，也未按教师姓名作为主授权依据。

## 12. 失败项

本轮失败项均为运行环境阻塞，不是新增代码构建失败：

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| gateway 运行态 | FAIL | 本机无监听，候选地址不可达 |
| auth 运行态 | FAIL | 本机无监听，候选地址不可达 |
| system 运行态 | FAIL | 本机无监听，候选地址不可达 |
| competition 运行态 | FAIL | 本机无监听，候选地址不可达 |
| PC 前端运行态 | FAIL | 本机无监听，未提供正式测试地址 |
| 测试库连接 | FAIL | `10.10.10.10:3306` 超时，本机 `3306` 未监听 |
| Nacos 连接 | FAIL | `10.10.10.10:8848` 超时 |
| 登录 token 获取 | FAIL | auth/system 不可达，账号未提供 |
| 接口联调 | BLOCKED | 无运行态和 token |
| PC 页面验证 | BLOCKED | 无运行态和账号 |

## 13. 修复建议

环境侧：

1. 部署或重启测试环境 gateway / auth / system / competition，并确认 competition 已加载本次代码；
2. 提供测试环境 PC 访问地址，或允许使用仓库 UAT 脚本启动完整本地 UAT 环境作为正式联调环境；
3. 恢复当前机器到测试库/Nacos 的网络访问，或提供可达的测试库/Nacos 地址；
4. 提供教师 A、教师 B、普通学生测试账号；
5. 准备符合本报告测试数据清单的团队、学生、证件数据。

数据侧待复核：

1. 若真实数据中 `competition_role_name = '指导老师'` 的教师报名行较多，建议后续补充授权 SQL 支持该角色名；
2. 若 `leader_teacher_id` 在历史数据中为空，但存在稳定教师用户 ID 字段，应补充该稳定字段；
3. 若线下导入学生未使用 `pay_status = paid` 或 `check_status = 4`，应先评估业务口径，再做最小范围修订；
4. 不建议把 `guide_teacher` 姓名模糊匹配作为主授权依据。

代码侧：

- 本轮不建议直接修改代码，因为尚未拿到真实测试库证据；
- 待运行态恢复后优先按现有实现完成正向、越权、安全字段和 PC 页面验证。

## 14. 是否可以验收通过

不可以。

原因：

- 本轮未完成真实测试库联调；
- 未验证教师授权关系；
- 未验证学生列表和无证状态；
- 未验证证件详情和二维码展示；
- 未验证 teamCode / credentialId 越权拦截；
- 未验证 PC 页面展示；
- 未验证真实响应安全字段。

当前状态：运行态阻塞，待测试环境恢复后重新执行联调。
