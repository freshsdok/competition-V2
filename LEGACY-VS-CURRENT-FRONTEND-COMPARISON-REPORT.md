# V2 历史 Human UX 原型与现行前端比较报告

- 审查日期：2026-08-28（Asia/Shanghai）
- 审查性质：只读比较，不实施修改
- 历史原型：`admin-demo (16).html`、`web-demo (16).html`
- 指令包：`V2-LEGACY-VS-CURRENT-FRONTEND-COMPARISON-v1.0.zip`
- 现行基线：`/Users/wwang/Documents/ClaudeCode/deshi_competition_v2`
- Git 快照：分支 `deshi_mac`，HEAD `5b324e0`
- 明细矩阵：`FRONTEND-COMPARISON-MATRIX.csv`（59 项）

## 0. 审查边界、事实与限制

### 事实

1. ZIP 内指令明确要求 `COMPARISON ONLY`：旧 HTML 只作为历史 Human UX 原型；现行 Repository 代码是当前前端基线；禁止据旧原型修改后端、Root Model 或现有前端。
2. 本轮仅检查 Admin、Web、H5 的信息架构、人类语言、任务流、视觉模式、状态反馈以及模型演进影响；未审计后端正确性、数据库、API 合同、生产就绪或发布状态。
3. 现行 Repository 在审查时不是干净工作树。前端有 4 个既存修改文件，集中于费用/票据能力：
   - `frontend/apps/admin/src/features/commerce/CommerceFinancePanel.test.ts`
   - `frontend/apps/admin/src/features/commerce/CommerceFinancePanel.vue`
   - `frontend/apps/web/src/features/commerce/CommerceCenterView.vue`
   - `frontend/packages/api-client/src/commerce.ts`
4. 因用户指定“当前 Repository 代码”为现行基线，本报告采用 **HEAD `5b324e0` + 当时工作树中的现行前端内容**，不回退、不覆盖，也不把脏工作树误称为已提交版本。
5. 两个历史 HTML 均以本地浏览器实际打开：Admin 可导航至组织管理和赛事六步构建器；Web 可完成模拟验证码登录、进入工作台和查看工作身份；浏览器未记录 JavaScript error/warn。404 favicon 不影响页面功能。
6. 历史原型自身明确出现“演示器 · 非产品”“原型先行”“待后端支持”“非本版本范围”等标签，且使用静态数字、角色模拟和成功 Toast。
7. 输入文件的 SHA-256 证据指纹为：
   - `admin-demo (16).html`：`7f636bd971fbf43df49e05c42e13894689de4739cedeba96691021597983564a`
   - `web-demo (16).html`：`03dbded465aae35112663618dfc0fd1b8b06a0f02c624bb6cce6a9138988dfdf`
   - ZIP 指令包：`ce6c92d7e07d1d11e737478ab8002ffc8b3ce161b2d94481f4b3fc4157e3de3c`

### 推理

1. 历史 HTML 能证明的是交互理念、信息组织和理想路径，不证明后端能力、授权正确性或业务操作真的完成。
2. 现行前端的模型语义以当前源代码、路由、组件和项目状态文件为证据；旧 HTML 中关于后端字段或旧设计文档的说明只是历史快照，不能覆盖当前实现。
3. 组织关系、有效行政成员、`EXACT/DESCENDANTS`、Human Model、Survey/Form、Feedback/Complaint、typed settings、H5 等后续能力，应归为 `MODEL_EVOLUTION`，不能倒推为旧原型设计失败。

### 风险与证据限制

1. 现行 Repository 正在变化，且包含未提交前端修改；本报告只对上述时间点负责。
2. 本轮未启动现行全栈服务，因此未声称“现行页面已通过端到端验收”。现行侧结论来自源代码、路由、组件和已有状态证据；历史侧另做了浏览器交互核验。
3. 历史 HTML 的静态数据、按钮成功反馈、角色切换、接口文字均不能作为真实业务或授权证据。
4. 本报告的“未来参考”只表示可记忆的 UI 模式，不是开发任务、整改清单、迁移方案或发布建议。

## 1. Executive comparison

总体结论：**历史原型与现行项目在核心用户旅程上大体一致，但它们处于不同模型阶段。** 赛事发现、报名、工作台、材料/提交、评审、现场、预约、费用、证书、CMS 和赛事六步构建等主任务有明显连续性；现行前端没有背离原型的“以任务为中心、用人类语言解释状态”的基本理念。

差异主要来自两个方向：

- 历史原型更像完整的 Human UX 展示稿：页面密度高、导航入口集中、卡片与数字丰富、步骤和时间线直观，适合演示与快速扫描。
- 现行前端更像受模型和真实状态约束的产品基线：明确业务/技术模式、真实会话与服务端投影、组织关系与授权解耦、失败关闭、版本化状态、G2/G3 参与域以及独立 H5。

59 项矩阵的分类分布如下：

| 分类 | 数量 | 含义 |
|---|---:|---|
| `MODEL_EVOLUTION` | 20 | 差异来自后续 Root/Human/组织/参与域或多端模型演进 |
| `MIXED` | 16 | 历史 UX 表达和现行真实性各有优势 |
| `CURRENT_STRONGER` | 13 | 现行在边界、状态、模型或可访问性方面更强 |
| `SAME` | 4 | 核心任务与结构基本一致 |
| `LEGACY_STRONGER` | 3 | 历史原型在纯 UX 呈现上更成熟 |
| `OBSOLETE_LEGACY` | 3 | 历史做法不应恢复 |
| `CURRENT_GAP` | 0 | 未找到足以在本轮证据下确认的现行功能缺口 |

这里的 `CURRENT_GAP = 0` 不表示现行前端完美，而表示本轮没有把“旧原型更好看/更密集”误写成现行功能缺陷；已确认的视觉差距被保守地归为 `LEGACY_STRONGER` 或 `MIXED`。

## 2. Admin similarities

1. **赛事中心主路径一致。** 两者都有租户/单位上下文、赛事列表、状态、进入赛事工作区和构建配置入口。
2. **六步构建理念高度一致。** 公开信息 → 组别与报名规则 → 资格要求 → 报名材料 → 赛事阶段与成果提交 → 预览与发布，在历史原型和现行 `CompetitionBuilder` 中均成立。
3. **运营域连续。** CMS、成员/报名、材料、评审、现场、预约、费用和沟通均有明确管理入口。
4. **人类任务语言连续。** 两者均以“待审核”“去处理”“当前单位”“当前赛事”“保存本步”等业务语言组织操作。
5. **上下文先于操作。** 历史原型用租户和赛事选择器，现行版用工作区和活动上下文；两者都避免完全无上下文的业务操作。

## 3. Admin differences

### 3.1 信息架构

历史 Admin 把平台治理、组织运营和租户运营同时铺在左侧栏，入口可发现性强；现行 Admin 进一步区分 business/technical 模式，并根据当前活动显示交付、现场和费用区域。历史版更适合全景演示，现行版更能控制操作范围。

### 3.2 组织模型

历史原型采用“学校是唯一运营主体，学院只是归属标签”的简化模型，并明确标注这是“原型先行”。现行版已转向 `OrganizationRelation`：

- 主行政树与非行政关联并存；
- 关系有类型、主父节点、有效期和历史时点；
- “进树 ≠ 有权”，组织关系不直接等同授权；
- 区分直接成员、有效行政成员、当前管理员和次级关系；
- 支持 `EXACT/DESCENDANTS` 范围；
- 移动前提供影响预览并保留关系历史。

因此，这不是现行偏离原型，而是模型演进。旧版学校—学院树的视觉易读性仍有参考价值，但其权限和成员语义已经过时。

### 3.3 状态与失败处理

历史原型擅长徽章、统计卡、Toast 和步骤提示，但多为静态成功路径。现行组件普遍区分 loading、empty、error、冲突、未完成和成功，并在费用工作台中明确表达“操作暂未完成；当前记录不会被当作成功处理”。现行状态可信度更高。

### 3.4 领域拆分

历史原型倾向一个入口覆盖整条运营链；现行版把 Submission、Artifact、Evaluation、AwardDecision、Runtime、Attendance、ResourceReservation、Communication、Engagement 等职责拆开。旧版跨域总览更顺手，现行版领域边界更清楚。

### 3.5 平台治理

现行版新增安全 Bootstrap、typed settings、设置继承/影响预览以及真实插件治理工作区。旧版“插件与扩展”已标为非本版本范围，其静态/市场化暗示不适合作为现行目标。

## 4. Web similarities

1. **公开到个人的旅程一致。** 首页/资讯 → 赛事发现与详情 → 报名 → 工作台 → 材料/提交/评审/现场/费用/证书。
2. **工作台的任务优先级理念一致。** 历史原型用“需要处理、重要信息、当前状态”；现行 `WorkspaceBoard` 仍以服务器投影的任务区块和“去处理”动作组织页面。
3. **个人中心结构连续。** 我的信息、材料、提交、到场、现场任务、预约、证书、邀请、隐私等入口在现行路由中均有对应或更细分能力。
4. **状态驱动 CTA。** 两者都不是只展示信息，而是把下一步操作放在状态卡中。
5. **人类可读提示。** 截止时间、待处理原因、当前单位和任务对象都被置于主要信息层。

## 5. Web differences

### 5.1 公共门户

历史 Web 的 Hero、资讯卡、赛事卡、帮助 CTA 默认内容饱满，首屏叙事更强。现行门户使用服务器驱动的 HERO、ACTIVITY_CARDS、ARTICLE_LIST、RICH_TEXT、QUICK_LINKS 和 PROMO_BANNER，内容和租户上下文更可靠，但默认视觉密度较低。

### 5.2 工作台投影

历史版用固定数据同时演示学生、教师、评审和组织管理员场景；现行版只展示服务端投影的区块与动作，并在执行时重新鉴权。旧版适合说明产品全貌，现行版更符合真实可见范围。

### 5.3 身份与工作单位

历史版“工作身份”下拉直观，但容易被理解为用户可以切换权限；历史工作台还出现“接受后创建身份并授予角色”的邀请语义。现行版明确分离账户、主体、身份、成员关系和资格，并说明切换工作单位是导航上下文，不是权限来源。旧版角色/身份快捷授予属于 `OBSOLETE_LEGACY`。

### 5.4 参与域

历史版主要提供“投诉与举报”。现行 `Engagement` 已分离并扩展为：

- feedback / complaint / suggestion；
- 严重级别、关联会话、回执和状态；
- 待填问卷；
- 多种题型、条件显示、矩阵和文件；
- 发布版本、关闭、归档和导出。

这是 G2/G3 模型演进，不是旧原型遗漏需要补做。

### 5.5 非理想路径

历史原型主要呈现成功与预置数据；现行版显式处理加载、空、失败和能力缺口。现行更能避免用户把“按钮已点”误解为“业务已完成”。

## 6. Model evolution

以下差异必须作为模型演进解释，不能把旧 HTML 当作规范倒压现行系统：

1. **OrganizationRelation：** 从固定学校—学院标签树演进为带类型、时间和历史的关系图。
2. **行政范围：** 新增 `EXACT/DESCENDANTS`，并用人类语言解释作用范围。
3. **Effective Administrative Membership：** 成员关系可以区分直接、继承、有效来源。
4. **授权边界：** 组织关系、成员关系、身份、角色和资格不再被一个“工作身份”控件混合。
5. **G2 Human Model UX：** 账户、主体、身份、成员、资格和人与组织关系分层展示。
6. **G3 Engagement：** Feedback/Complaint 与 Survey/Form 成为独立、可追踪、版本化的参与域。
7. **Settings：** 从普通配置面板演进为 typed settings、继承和影响预览。
8. **Plugin：** 现行是九类编译期、受治理的插件能力，不是动态插件市场。
9. **Bootstrap：** 新增一次性、安全的初始化边界。
10. **H5：** 从响应式 Web 原型演进为具有独立路由、四 Tab 和 27 个页面的移动交付面。

## 7. Legacy patterns worth remembering

以下是历史原型中值得保留为设计记忆的模式，不代表立即实施：

1. 左侧导航的领域分组、待办数字和清晰图标。
2. 首页/工作台把“需要处理、重要信息、当前状态”分成三层。
3. 赛事卡同时展示状态、主办方、截止时间和单一主 CTA。
4. 六步构建器的横向进度条、当前步骤说明和上下步关系。
5. 资源预约中的资源卡—时间段—操作三段式布局。
6. 现场运行把态势摘要、签到和现场任务放在同一场景语言中。
7. 费用页将金额、截止时间和票据/退款入口置于首要视觉层。
8. 组织树用缩进、全路径和汇总数字降低理解成本。
9. 个人中心用“基本信息 / 工作身份 / 账号与安全”标签降低首次理解成本。
10. 统一的状态徽章、空白间距和卡片标题使高密度页面仍可扫描。

## 8. Current improvements to preserve

以下现行优势不应被视觉回溯削弱：

1. 业务模式与技术模式的明确隔离。
2. 服务端投影、受控动作目录和操作时重新鉴权。
3. 组织关系与授权分离，以及“进树 ≠ 有权”的明确提示。
4. 直接/继承/有效成员来源可解释。
5. `EXACT/DESCENDANTS`、设置继承和移动影响预览。
6. Human Model 对账户、主体、身份、成员、资格的分层。
7. 报名草稿、提交、审批、费用门禁和最终确认不混为一个“成功”。
8. 加载、空、失败、冲突、未完成和成功状态的显式区分。
9. Feedback/Complaint、Survey/Form 和 Communication 的领域分离。
10. 费用操作的因果链、红冲/退费影响和失败关闭语言。
11. 编译期插件治理边界，不暗示动态插件市场。
12. 独立 H5 的移动任务覆盖和四 Tab 信息架构。

## 9. Legacy patterns NOT to revive

1. **一键角色切换作为产品能力。** 历史页面已自称“演示器 · 非产品”；现行授权不能由前台演示控件替代。
2. **接受普通邀请即创建身份并授予角色。** 该语义与现行身份、资格和授权治理边界冲突。
3. **学校唯一运营主体、学院永远只是标签。** 这会覆盖现行 `OrganizationRelation` 与有效成员模型。
4. **组织树等同权限树。** 现行必须保留“关系不等于授权”。
5. **静态成功 Toast 代表业务成功。** 只有真实状态结果可以确认成功。
6. **把 API 路径、字段名或“待后端支持”直接暴露给普通用户。** 这些内容在历史原型中用于说明设计状态，不是终端 UX 标准。
7. **动态插件市场暗示。** 现行冻结边界是受治理的编译期插件家族。
8. **用固定演示数字代替加载、空、失败和能力缺口。** 现行的非理想路径必须保留。

## 10. Concise future UI-polish reference list

仅作为以后进行 UI 精修时的参考索引：

- Admin：分组侧栏、当前上下文条、待办徽章、六步构建器进度条、组织树视觉投影、跨域现场摘要、资源时段卡、费用总览卡。
- Web：首页 Hero + 赛事/资讯双轨、三层任务工作台、赛事状态卡、个人中心标签、材料与提交互链、预约时段选择、缴费截止提示、证书成果卡。
- H5：保持现行四 Tab 和单任务短路径，可复用历史原型的卡片层次与状态色，但不复用角色模拟或静态成功语义。
- 通用：所有视觉增强必须承载现行模型、真实状态、可访问语义和失败关闭；历史原型只提供表现模式，不提供权限、后端或 Root Model 规范。

## 审查结论

**比较完成，边界符合 ZIP 指令。** 两个历史 HTML 与现行项目在主任务理念上总体一致；最显著的差异来自模型演进，而不是现行前端偏离。历史原型在视觉层次、扫描效率、任务引导和演示完整度上有若干可记忆优势；现行前端在模型准确性、授权边界、状态真实性、参与域和移动端覆盖上明显更强。

本轮未修改后端、Root Model 或现有前端，未创建实施任务，未形成迁移或发布建议。
